# Excel emulator mine — clock-aware cycle

_Generated 2026-09-07 · live `flatten_robust` is not changed._

Inventory of stored A–O vs full A–JL: `excel_bot/research/GRID_INVENTORY.md`.
This file is overwritten by `python excel_bot/engine/mine_clock.py` after
grids are rebuilt. Until then: **no keeper table** — backtest pending.

## Ship bar

PASS needs discovery n≥300 t≥3 avg>0; holdout n≥100 t≥2 avg>0; ≥50 tickers;
≥20 dates; no one-trade lottery; both tape halves; SPY up and down when
split is thick enough; hold1 only if hold2 also PASSes. Clock on every row.

Research only. No merge without Cyrus.
