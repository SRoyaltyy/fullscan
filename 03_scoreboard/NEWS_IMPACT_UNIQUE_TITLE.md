# News-impact unique-title book

Deterministic router on **every unique title** (all sources, all dates). Not the 09-17 slice. Not only the old 94 taped rows. No Lane. Research-only.

## Scoreboard

Pending restamp (`python3 -m src.news_impact.unique_title`). Harvest wire is in; fixtures live under `data/grok_automations/`.

- **grok_automations**: files=2 items=2 status=used (fixture path ready; coordinator dump tree is a follow-up)
- **unique n / graded n / converge vs singleton**: restamp fills these. No fake five digits.

See `docs/GROK_AUTOMATIONS_HARVEST.md`.
