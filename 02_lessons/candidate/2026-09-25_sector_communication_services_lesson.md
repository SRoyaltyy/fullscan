---
trigger_pattern: "A sector card whose own factor sum is clearly negative (S0–S3 all signed −1, leading sum ≤ −4) and whose own prose explicitly concludes "down, not flat," on a session where the sector ETF is ABSENT from the injected premarket board (non-participating) while the broad futures sleeve is green (ES/NQ positive) — the deterministic engine's tape_anchor + index_carry component nets the negative factor sum toward zero and emits flat/flat, overriding the card's own divergence-resolution logic."
current_behavior: "The v2 engine computes total_score = (leading_sum × multiplier) + anchor + index_carry. On 2026-09-25 for XLC: leading_sum −7.0 (with skill multipliers) × 0.85 = −5.95, but anchor +2.55 and index_carry +0.676 netted the total to −0.386 → flat/flat. The card's prose said "trust the factors over the tape — the correct read is down, not flat," and the card explicitly warned "do not map NQ=F +0.57% / NDX +0.41% / XLK +0.79% onto this book." The engine mapped it anyway. This is the seventh consecutive overlay-created direction miss (09-15/16/17/18/22/23/24 → 09-25). XLC closed −0.90% / −1.45% rel vs SPY +0.54%."
corrected_behavior: "When divergence_flagged = TRUE AND the sector ETF is absent from the injected premarket board (non-participating sector), the anchor/index_carry component must be ZEROED or heavily damped (e.g., ×0.2), not netted at full weight against the factor sum. A green ES/NQ sleeve is not a participation certificate for a sector that is not on the board. The factor sum should dominate the direction call in this configuration. Additionally, when the card's own prose concludes a direction that contradicts the deterministic output, that contradiction should be surfaced as a hard flag requiring the engine to justify the override — not silently resolved in favor of the anchor."
evidence_cited: "2026-09-25 XLC: predicted flat/flat (total_score −0.386), actual −0.9036% / rel −1.45% (notable down). Card prose: "the correct read is down, not flat." Card S0–S3 all −1, S4 = 0, divergence TRUE. Anchor +2.55 (NQ +0.57%, ES +0.28%), index_carry +0.676. XLC absent from PM board. Scoreboard: dir=0.2 last 10, 0.222 last 30 — the misses cluster on exactly this configuration. Prior lessons 09-16/09-18/09-24 all describe the same anchor-override family."
error_category: "B"
falsifier: "If a future session has divergence_flagged = TRUE, the sector ETF absent from the PM board, and a negative factor sum (leading sum ≤ −3), yet XLC (or the analogous sector ETF) closes UP or flat (rel ≥ −0.3%) — then the "zero the anchor for non-participating sectors" rule is wrong and the green futures sleeve should be read as genuine participation. Conversely, if zeroing the anchor produces a down call that misses on a session where the sector was absent from the board but the broad tape carried it up, the rule is over-firing."
sector: "Communication Services"
date: "2026-09-25"
status: "candidate"
---

# Sector Reflection — Communication Services — 2026-09-25

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: A sector card whose own factor sum is clearly negative (S0–S3 all signed −1, leading sum ≤ −4) and whose own prose explicitly concludes "down, not flat," on a session where the sector ETF is ABSENT from the injected premarket board (non-participating) while the broad futures sleeve is green (ES/NQ positive) — the deterministic engine's tape_anchor + index_carry component nets the negative factor sum toward zero and emits flat/flat, overriding the card's own divergence-resolution logic.
CURRENT_BEHAVIOR: The v2 engine computes total_score = (leading_sum × multiplier) + anchor + index_carry. On 2026-09-25 for XLC: leading_sum −7.0 (with skill multipliers) × 0.85 = −5.95, but anchor +2.55 and index_carry +0.676 netted the total to −0.386 → flat/flat. The card's prose said "trust the factors over the tape — the correct read is down, not flat," and the card explicitly warned "do not map NQ=F +0.57% / NDX +0.41% / XLK +0.79% onto this book." The engine mapped it anyway. This is the seventh consecutive overlay-created direction miss (09-15/16/17/18/22/23/24 → 09-25). XLC closed −0.90% / −1.45% rel vs SPY +0.54%.
CORRECTED_BEHAVIOR: When divergence_flagged = TRUE AND the sector ETF is absent from the injected premarket board (non-participating sector), the anchor/index_carry component must be ZEROED or heavily damped (e.g., ×0.2), not netted at full weight against the factor sum. A green ES/NQ sleeve is not a participation certificate for a sector that is not on the board. The factor sum should dominate the direction call in this configuration. Additionally, when the card's own prose concludes a direction that contradicts the deterministic output, that contradiction should be surfaced as a hard flag requiring the engine to justify the override — not silently resolved in favor of the anchor.
EVIDENCE: 2026-09-25 XLC: predicted flat/flat (total_score −0.386), actual −0.9036% / rel −1.45% (notable down). Card prose: "the correct read is down, not flat." Card S0–S3 all −1, S4 = 0, divergence TRUE. Anchor +2.55 (NQ +0.57%, ES +0.28%), index_carry +0.676. XLC absent from PM board. Scoreboard: dir=0.2 last 10, 0.222 last 30 — the misses cluster on exactly this configuration. Prior lessons 09-16/09-18/09-24 all describe the same anchor-override family.
LESSON_MATCH_CHECK: Matches 2026-09-24_sector_communication_services_lesson (two-name-concentrated ETF, printed product event, PM red, rates tax binding — card signed fade/unwind) — same sector, same anchor-override family, but that lesson was about signing the printed product as fade; today's is about the engine netting a correctly-signed negative factor sum to flat. Also matches 2026-09-24_sector_healthcare_lesson (v2 emits down/mild from tape_anchor + index_carry while card zeros S4 so described cushion cannot offset carry) and 2026-09-24_sector_industrials_lesson (overlay fires score-vs-tape flatten / DO-INSTEAD instead of paying S0). The family is well-established; this is the cleanest instance.
BACKWARD_CHECK: Would this lesson have helped on 09-15/16/17/18/22/23/24? Yes — every one of those was an overlay-created direction miss where the card's factor read was directionally right and the anchor pulled the output toward flat/up. On 09-24 the card predicted down/notable and XLC rose +1.27% (the one case where the card's prose was wrong and the anchor was right) — but that was a *printed product continuation* day, not a non-participating-sector day, so the zeroing rule (conditioned on absence from PM board) would not have fired. The rule is correctly scoped: it fires only when the sector is non-participating, which is the configuration where the anchor is systematically wrong.
CONFLICT_CHECK: No conflict with 09-24_sector_communication_services_lesson (that lesson says sign the printed product as fade when PM is red — compatible; today PM was absent, not red, and the fade was still correct). No conflict with 09-24_sector_energy_lesson (energy up/mild on green barrel — different configuration, sector was the rotation destination, not absent). No conflict with 09-24_sector_real_estate or utilities (down/mild on signed hawkish S0 with confirming relative lag — those were dir HITs, the anchor did not override). The rule is additive: it only changes behavior when the sector is absent from the PM board AND divergence is flagged.
FALSIFIER: If a future session has divergence_flagged = TRUE, the sector ETF absent from the PM board, and a negative factor sum (leading sum ≤ −3), yet XLC (or the analogous sector ETF) closes UP or flat (rel ≥ −0.3%) — then the "zero the anchor for non-participating sectors" rule is wrong and the green futures sleeve should be read as genuine participation. Conversely, if zeroing the anchor produces a down call that misses on a session where the sector was absent from the board but the broad tape carried it up, the rule is over-firing.
DIVERGENCE_VERDICT: leading_right
ACTIVE_LESSON_REVIEW: The 09-24 Communication Services lesson (printed product event → fade when PM red) was directionally correct in spirit but the card under-modeled Meta Connect (09-25) as a *fresh* event-day binary — the card treated Muse/Connect as "T+2, spent" and applied the 09-23 "crowded-long-into-after-cash-binary is SPENT" rule, missing that Connect itself was a live 09-25 event. The sell-the-news reversal (META 52-week high at event open → −3.1% close) was knowable at the open. This is a secondary lesson: scheduled event-day + crowded-long + at-highs → negative skew must be modeled as a distinct factor. The primary lesson (anchor override) remains the dominant failure and is now confirmed for the seventh consecutive session.
SECTOR: Communication Services
LESSON_END
