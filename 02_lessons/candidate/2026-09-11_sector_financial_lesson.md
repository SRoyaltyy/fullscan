---
trigger_pattern: "A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open, and the model scores a component (S4 ETF tape) on a *stale, sub-gate* prior-day relative print (|1d rel| below the sector's own confirmation gate, e.g. +0.27% < the 08-18 ≥ +0.4% gate), producing a false "mild divergence" that is then resolved by "trusting the live macro over the modest 1d tape" — while the live macro itself is the pending binary, whose resolution is unknowable at the open."
current_behavior: "S4 was scored +0.5 on a 1d rel of +0.27% (below the ≥ +0.4% confirmation gate) with a same-day CPI binary pending. This created a leading-sum-0 vs S4-+0.5 divergence that the morning resolved toward the positive side ("trust the live macro"), implying XLF would at least match SPY. XLF instead underperformed by −0.18% and the magnitude band missed (flat called, mild actual)."
corrected_behavior: "When a same-day high-impact binary is pending AND the freshest 1d relative print is below the sector's own confirmation gate, set S4 ≈ 0 (treat the sub-gate rel as noise, not signal). Do not manufacture a divergence from a sub-gate tape and then resolve it in the direction of the pending binary's benign branch. Additionally, do not net an *input* to the binary (oil, which feeds CPI) against the binary itself as if they were independent votes — hold S0 at a small skew rather than a symmetric zero when the sector's 3d/1w relative tape is already red and the long end is in the stress zone."
evidence_cited: "Morning S4 = +0.5 on 1d rel +0.27% (sub-gate); self-audit explicitly flagged "mild divergence; trust the live macro over the modest 1d tape." Actual: XLF +0.67% vs SPY +0.85%, rel −0.18%; direction flat vs actual up (dir MISS per scoreboard), magnitude flat vs actual mild (mag MISS). Ex-post fair S4 ≈ 0, S0 ≈ −0.5 (hot CPI 0.4% m/m / 3.4% y/y, hike odds up). The 09-10 S1 cap was vindicated (S1 = 0 correct; no double-count)."
error_category: "B"
falsifier: "If, on a future session with a pending high-impact binary and a sub-gate 1d rel (< +0.4%), the sector ETF *matches or outperforms* SPY and the magnitude band is hit with S4 scored ≈ 0, the claim that sub-gate rel should be zeroed on binary days would be weakened. Conversely, if scoring S4 positive on a sub-gate rel on a binary day repeatedly produces relative-underperformance misses, the lesson strengthens."
sector: "Financial"
date: "2026-09-11"
status: "candidate"
---

# Sector Reflection — Financial — 2026-09-11

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open, and the model scores a component (S4 ETF tape) on a *stale, sub-gate* prior-day relative print (|1d rel| below the sector's own confirmation gate, e.g. +0.27% < the 08-18 ≥ +0.4% gate), producing a false "mild divergence" that is then resolved by "trusting the live macro over the modest 1d tape" — while the live macro itself is the pending binary, whose resolution is unknowable at the open.
CURRENT_BEHAVIOR: S4 was scored +0.5 on a 1d rel of +0.27% (below the ≥ +0.4% confirmation gate) with a same-day CPI binary pending. This created a leading-sum-0 vs S4-+0.5 divergence that the morning resolved toward the positive side ("trust the live macro"), implying XLF would at least match SPY. XLF instead underperformed by −0.18% and the magnitude band missed (flat called, mild actual).
CORRECTED_BEHAVIOR: When a same-day high-impact binary is pending AND the freshest 1d relative print is below the sector's own confirmation gate, set S4 ≈ 0 (treat the sub-gate rel as noise, not signal). Do not manufacture a divergence from a sub-gate tape and then resolve it in the direction of the pending binary's benign branch. Additionally, do not net an *input* to the binary (oil, which feeds CPI) against the binary itself as if they were independent votes — hold S0 at a small skew rather than a symmetric zero when the sector's 3d/1w relative tape is already red and the long end is in the stress zone.
EVIDENCE: Morning S4 = +0.5 on 1d rel +0.27% (sub-gate); self-audit explicitly flagged "mild divergence; trust the live macro over the modest 1d tape." Actual: XLF +0.67% vs SPY +0.85%, rel −0.18%; direction flat vs actual up (dir MISS per scoreboard), magnitude flat vs actual mild (mag MISS). Ex-post fair S4 ≈ 0, S0 ≈ −0.5 (hot CPI 0.4% m/m / 3.4% y/y, hike odds up). The 09-10 S1 cap was vindicated (S1 = 0 correct; no double-count).
LESSON_MATCH_CHECK: Matches the 2026-09-11_lesson.md candidate (pending binary + futures-confirmed direction + narrative lean agreeing with futures but pipeline mapping to flat) and the 2026-09-11_sector_communication_services_lesson.md candidate (single stale rel print used to justify a positive component score without fresh same-day confirmation). It is the *financial-sector instantiation* of the stale-rel-print family, with the added twist that the stale print was used to build a divergence resolved toward the binary's benign branch. Distinct from the 09-10 Financial lesson (which was about *not* stacking S1 on a flat rel) — that lesson held and is confirmed here.
BACKWARD_CHECK: Had S4 been set to ≈ 0 (sub-gate rel treated as noise) and S0 held at a small negative skew (oil subordinate to CPI, sector's 3d/1w rel already red, 30Y 5.28% stress), the leading sum would have been ≈ −0.5 with no manufactured positive divergence. The pipeline total would have mapped to flat-to-mildly-down rather than a clean flat with a positive S4 lean — closer to the actual mild-up/relative-underperform outcome and removing the wrong-signed relative implication. The direction call (flat) would likely have remained a soft hit; the magnitude band would have been better positioned. Backward test passes: the correction reduces error without requiring knowledge of the print.
CONFLICT_CHECK: No conflict with the 09-10 Financial lesson (S1 cap) — that lesson is confirmed and should be kept. No conflict with 08-28 (anti-triple-count) or 08-17 (long-end steepener ≠ NIM+). Potential tension with the 09-04 lesson (pre-score asymmetric risk when a binary is pending): the 09-04 lesson says *skew*, this lesson says *don't skew positive on a stale sub-gate tape* — these are compatible (both argue against a clean symmetric zero resolved toward the benign branch). No conflict with the 08-18 ≥ +0.4% gate — this lesson *enforces* it by extending it to S4 scoring on binary days.
FALSIFIER: If, on a future session with a pending high-impact binary and a sub-gate 1d rel (< +0.4%), the sector ETF *matches or outperforms* SPY and the magnitude band is hit with S4 scored ≈ 0, the claim that sub-gate rel should be zeroed on binary days would be weakened. Conversely, if scoring S4 positive on a sub-gate rel on a binary day repeatedly produces relative-underperformance misses, the lesson strengthens.
DIVERGENCE_VERDICT: futures_right — the green futures (ES +0.63%, NQ +0.65%) correctly anticipated the *absolute* up move (XLF +0.67%), but the sector's relative underperformance (−0.18%) was driven by the hot-CPI resolution, which was not knowable at the open. The morning's divergence resolution ("trust the live macro") was directionally right on absolute but wrong on relative sign.
ACTIVE_LESSON_REVIEW: Keep the 09-10 Financial S1 cap (confirmed, prevented double-count). Keep 08-28 anti-triple-count (S2/S3 = 0 correct). Keep 08-17 (long-end steepener scored in S0 context only). Keep 08-18 gate (off today at +0.27%; this lesson extends its logic to S4). Add: (1) zero S4 on sub-gate rel when a binary is pending; (2) treat oil as subordinate to CPI, not an independent offset — hold S0 at a small skew rather than symmetric zero when 3d/1w rel is red and the long end is stressed; (3) add the gap-and-fade-while-SPY-holds pattern as a live intraday tell for a hawkish-rate fade in financials.
SECTOR: Financial
LESSON_END
