---
trigger_pattern: "A sector card whose factor sum is net-negative (S0–S4 ≤ −4) and whose own prose concludes "no divergence — factors and tape agree down," on a session where index futures are green (ES/NQ positive), VIX is low and in contango, and the sector ETF is mega-cap-concentrated (top-2 holdings ≳40% of book) — the card emits an absolute down/mild call, but the realized print is flat-to-up absolute with the negative sum expressing only as relative underperformance."
current_behavior: "The card conflates the relative call with the absolute call. It scores the rates/mortgage/credit channel as a signed negative S0/S1, correctly reads nested breadth as unanimous down (S2), and correctly leans on multi-horizon relative lag (S4) — then emits "down/mild" as if a net-negative factor sum on a duration-heavy book must produce a down *absolute* session. It explicitly discards green futures ("same discarded-anchor split as 09-16–09-24") and treats the 08-27 ban ("do not map XLK PM leadership into XLY") as covering the absolute-cushion question, when 08-27 is a *participation* rule, not an *absolute-floor* rule. The card has no term for "index beta provides an absolute cushion while relative still bleeds.""
corrected_behavior: "When the factor sum is net-negative but (a) index futures are green (ES and/or NQ positive), (b) VIX is low and in contango, and (c) the sector ETF is mega-cap-concentrated with top-2 holdings that are themselves mega-cap AI/index-beta names, the card must split the call: **flat absolute / down relative**, not down/mild absolute. Green NQ futures are a *sector* signal for a mega-cap-concentrated ETF whose top holdings are AI-adjacent — the 08-27 ban governs participation (do not infer sector breadth from XLK PM leadership), not the absolute floor. The 09-16 rule ("net-negative card + sub-gate 1d rel authorizes down but caps at mild") needs a companion: **when futures are green and VIX is low/contango, the absolute band is flat, not down.** Additionally, re-gate the "rates shock → down" mapping on whether the shock is *escalating* (new hawkish increment) or *persistent* (same level, no new news): a persistent level shock transmits as a relative grind, not an absolute break."
evidence_cited: "Predicted down/mild (total_score −3.2, S0 −1, S1 −1, S2 −1, S3 0, S4 −1, divergence_flagged True in pipeline but card self-audit said "no divergence"). Actual XLY +0.218% / SPY +0.544% / rel −0.326%. Direction MISS, magnitude MISS. Card's own Channel 1 recorded ES +0.28% / NQ +0.57% vs prior close and VIX 15.38 in contango — both discarded. Outcome review: "the card's error was not in its factor reads — it was in failing to separate the relative call from the absolute call." S2/S3/S4 were right; S0/S1 were right on relative, magnitude-overstated on absolute. The relative call (−0.33%) was fully knowable at open; the absolute flat-to-up print was also knowable from green futures + low VIX, which the card chose not to weight."
error_category: "A"
falsifier: "This lesson is falsified if a future sector card with (a) net-negative factor sum ≤ −4, (b) green ES/NQ futures, (c) VIX low/contango, and (d) mega-cap-concentrated ETF (top-2 ≳40%) produces a *down absolute* session of mild-or-greater magnitude — i.e., if the green-futures/low-VIX cushion fails to hold the absolute print flat. It is also falsified if the same setup produces a *down relative* print that is notable-or-greater (≥ ~0.5%), which would mean the relative lean was under-signed rather than the absolute band over-signed."
sector: "Consumer Cyclical"
date: "2026-09-25"
status: "candidate"
---

# Sector Reflection — Consumer Cyclical — 2026-09-25

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A sector card whose factor sum is net-negative (S0–S4 ≤ −4) and whose own prose concludes "no divergence — factors and tape agree down," on a session where index futures are green (ES/NQ positive), VIX is low and in contango, and the sector ETF is mega-cap-concentrated (top-2 holdings ≳40% of book) — the card emits an absolute down/mild call, but the realized print is flat-to-up absolute with the negative sum expressing only as relative underperformance.
CURRENT_BEHAVIOR: The card conflates the relative call with the absolute call. It scores the rates/mortgage/credit channel as a signed negative S0/S1, correctly reads nested breadth as unanimous down (S2), and correctly leans on multi-horizon relative lag (S4) — then emits "down/mild" as if a net-negative factor sum on a duration-heavy book must produce a down *absolute* session. It explicitly discards green futures ("same discarded-anchor split as 09-16–09-24") and treats the 08-27 ban ("do not map XLK PM leadership into XLY") as covering the absolute-cushion question, when 08-27 is a *participation* rule, not an *absolute-floor* rule. The card has no term for "index beta provides an absolute cushion while relative still bleeds."
CORRECTED_BEHAVIOR: When the factor sum is net-negative but (a) index futures are green (ES and/or NQ positive), (b) VIX is low and in contango, and (c) the sector ETF is mega-cap-concentrated with top-2 holdings that are themselves mega-cap AI/index-beta names, the card must split the call: **flat absolute / down relative**, not down/mild absolute. Green NQ futures are a *sector* signal for a mega-cap-concentrated ETF whose top holdings are AI-adjacent — the 08-27 ban governs participation (do not infer sector breadth from XLK PM leadership), not the absolute floor. The 09-16 rule ("net-negative card + sub-gate 1d rel authorizes down but caps at mild") needs a companion: **when futures are green and VIX is low/contango, the absolute band is flat, not down.** Additionally, re-gate the "rates shock → down" mapping on whether the shock is *escalating* (new hawkish increment) or *persistent* (same level, no new news): a persistent level shock transmits as a relative grind, not an absolute break.
EVIDENCE: Predicted down/mild (total_score −3.2, S0 −1, S1 −1, S2 −1, S3 0, S4 −1, divergence_flagged True in pipeline but card self-audit said "no divergence"). Actual XLY +0.218% / SPY +0.544% / rel −0.326%. Direction MISS, magnitude MISS. Card's own Channel 1 recorded ES +0.28% / NQ +0.57% vs prior close and VIX 15.38 in contango — both discarded. Outcome review: "the card's error was not in its factor reads — it was in failing to separate the relative call from the absolute call." S2/S3/S4 were right; S0/S1 were right on relative, magnitude-overstated on absolute. The relative call (−0.33%) was fully knowable at open; the absolute flat-to-up print was also knowable from green futures + low VIX, which the card chose not to weight.
LESSON_MATCH_CHECK: The 2026-09-25_sector_basic_materials_lesson is the closest structural match — a card with a mildly-negative factor sum, green live tape (ES/NQ positive, VIX calm/contango), and a divergence flag that neutralizes the negative sum into a flat call. That lesson's failure mode is the *opposite* sign (it flattened when it should have stayed signed). Today's failure is the mirror: the card stayed signed down when the green tape + mega-cap concentration should have flattened the *absolute* band while preserving the *relative* down lean. The 2026-09-24_sector_consumer_cyclical_lesson ("no corrective trigger — realized as a ~30 bp fade at the flat/mild boundary") is the same seam recurring: yesterday the card got the direction right at the boundary; today the same boundary flipped the direction call. The 2026-09-24_sector_technology_lesson (long-duration XLK on hawkish-duration session, milder band named in self-audit but v2 emitted more extreme) is a partial match on the "self-audit names milder band, card emits more extreme" pattern. No existing lesson covers the specific "net-negative sum + green futures + mega-cap concentration → split absolute/relative" correction.
BACKWARD_CHECK: Would this lesson have improved the last 10 graded runs? 09-22 (predicted down/mild, actual +0.089% — MISS): a green-futures/low-VIX check would likely have flattened the absolute band, converting a direction MISS to a flat HIT. 09-24 (predicted down/mild, actual −0.298% — HIT): futures were not green and VIX was not flagged low/contango, so the new rule would not have fired — no harm. 09-23 (predicted flat/flat, actual −1.408% — MISS): the rule is about *not* over-signing down on green tape; it would not have fixed a flat call that missed a down move. 09-16/09-17/09-18/09-21 (flat/flat calls, mixed actuals): the rule does not apply to flat calls. Net: the rule would have converted 09-22 from MISS to HIT and left 09-24 unchanged — a strict improvement on the two most recent down/mild calls. It does not address the flat-call misses (09-16 through 09-23), which are a separate failure mode.
CONFLICT_CHECK: No conflict with active lessons. The 09-23 gate ("score the lean, do not net to zero") is preserved — this lesson does not net the down-lean to zero; it preserves the *relative* down call and only flattens the *absolute* band. The 09-24 magnitude lesson ("do not mint notable from a 30 bp seam") is reinforced. The 08-27 ban ("do not map XLK PM leadership into XLY") is clarified, not overridden: 08-27 governs participation/breadth inference, not the absolute floor. The 09-16 rule ("net-negative card + sub-gate 1d rel → down capped at mild") is extended, not contradicted: the new companion rule adds a flat-absolute condition on top of the mild cap. The 09-25 basic-materials lesson (divergence flag neutralizing a negative sum into flat) is the mirror-image failure and is compatible — both lessons point toward "green tape + negative sum → flat absolute," from opposite starting signs.
FALSIFIER: This lesson is falsified if a future sector card with (a) net-negative factor sum ≤ −4, (b) green ES/NQ futures, (c) VIX low/contango, and (d) mega-cap-concentrated ETF (top-2 ≳40%) produces a *down absolute* session of mild-or-greater magnitude — i.e., if the green-futures/low-VIX cushion fails to hold the absolute print flat. It is also falsified if the same setup produces a *down relative* print that is notable-or-greater (≥ ~0.5%), which would mean the relative lean was under-signed rather than the absolute band over-signed.
DIVERGENCE_VERDICT: none_flagged — the pipeline set divergence_flagged=True (leading_sum −7.0 vs tape_anchor +2.124), but the card's self-audit explicitly overrode it ("No divergence — factors and tape agree down"). The card was wrong to override: the pipeline's divergence flag was the correct signal, and the card's dismissal of green futures as a "discarded-anchor split" is precisely the error the flag was pointing at. The divergence was real and the card suppressed it.
ACTIVE_LESSON_REVIEW: The 09-23 all-zero-default lesson fired correctly (7 down-leaning signals scored as a lean, not netted to zero) and produced the correct *relative* call — keep it. The 09-24 magnitude lesson held (refused to mint notable from a 30 bp seam; actual rel −0.33% was mild) — keep it. The 09-16 keep-overlay-down rule partially fired and correctly capped the band at mild — but it lacks the companion flat-absolute condition this lesson adds. The 08-27 NVDA/XLK-map ban fired as a ban on S0=+1 and was correct as a *participation* rule, but was misapplied as an *absolute-floor* rule — clarify scope. The 08-28 inherited-lag rule correctly did not fire as a suppression (S0 signed negative → 1w/1m lag is confirmatory trend) — keep it. The 09-25 basic-materials lesson is the mirror-image failure and should be cross-referenced. No active lesson is contradicted; one (08-27) needs a scope clarification.
SECTOR: Consumer Cyclical
LESSON_END
