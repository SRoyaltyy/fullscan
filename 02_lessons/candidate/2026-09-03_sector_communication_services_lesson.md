---
trigger_pattern: "Sector ETF prediction where the prior session had a >+1% relative gain (XLC +0.95% 1d rel vs SPY) and the current morning has (a) flat futures (ES +0.13% / NQ +0.16%), (b) no fresh sector-specific catalyst (ad/AI thesis carried, not new), and (c) the model scores all factors zero and predicts flat/flat — but the actual session delivers a +0.85% absolute / −0.19% relative day because the follow-through rule was applied asymmetrically: it capped magnitude at mild but did not consider that a flat-futures morning after an outsized relative day can still produce a modest absolute gain when the broad market rips (+1.05% SPY) on a macro catalyst (ISM Services beat) that the model explicitly identified as two-sided and refused to pre-score."
current_behavior: "When S0=S1=S2=S3=S4=0 and futures are flat, the model emits flat/flat with multiplier 0.9, treating "no fresh catalyst" as symmetric evidence against both up and down. The model identifies an upcoming macro print (ISM Services 10:00) as "two-sided" and refuses to pre-score it, but then also fails to account for the asymmetry that a beat would lift the whole market (including XLC's mega-cap growth book) while a miss would be cushioned by XLC's duration/growth character and the prior day's relative strength."
corrected_behavior: "When the model has explicitly identified a same-day macro catalyst (ISM Services, NFP, CPI) as "two-sided" but the sector is a mega-cap growth/duration book with (a) prior-day relative strength ≥ +0.9%, (b) real yields not rising same-morning, and (c) no fresh sector-specific negative — the flat call should carry a directional tilt toward up/mild (not flat/flat) because the asymmetry favors upside: a macro beat lifts mega-cap growth participation, while a macro miss is partially cushioned by duration relief (falling yields help the long-duration book). The "two-sided" framing is only symmetric for the SPX; for XLC specifically, the duration/growth composition makes the ISM outcome positively skewed (beat = risk-on participation, miss = duration relief). Additionally, when the model refuses to pre-score a macro print, it should still apply a small positive S0 tilt (e.g., +0.5 to +1) for a mega-cap growth sector with prior-day relative strength, rather than forcing exactly zero."
evidence_cited: "2026-09-03: predicted flat/flat (total 0.0), actual XLC +0.85% / SPY +1.05% / rel −0.19%. The model explicitly noted "ISM Services 10:00 ET (cons ~54.2 vs 54.1)" and "Soft labor/services are two-sided for this book (duration relief vs growth scare)" but scored S0=0. The actual ISM Services printed ~55.4 (beat), SPY ripped +1.05%, and XLC gained +0.85% — a flat call missed both direction and magnitude. The model's own Channel 2 text identified the asymmetry risk ("duration relief vs growth scare") but the scoring rubric forced a symmetric zero."
error_category: "A"
falsifier: "A future session where: (a) prior-day XLC relative gain ≥ +0.9%, (b) same-day ISM Services or NFP scheduled, (c) futures flat, (d) model applies +0.5 to +1 S0 tilt for mega-cap growth — but the macro print misses badly, SPY sells off >1%, and XLC falls >0.5% (duration relief fails to cushion because the growth scare dominates). If this happens, the directional tilt should be reverted to symmetric zero."
sector: "Communication Services"
date: "2026-09-03"
status: "promoted"
---

# Sector Reflection — Communication Services — 2026-09-03

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: Sector ETF prediction where the prior session had a >+1% relative gain (XLC +0.95% 1d rel vs SPY) and the current morning has (a) flat futures (ES +0.13% / NQ +0.16%), (b) no fresh sector-specific catalyst (ad/AI thesis carried, not new), and (c) the model scores all factors zero and predicts flat/flat — but the actual session delivers a +0.85% absolute / −0.19% relative day because the follow-through rule was applied asymmetrically: it capped magnitude at mild but did not consider that a flat-futures morning after an outsized relative day can still produce a modest absolute gain when the broad market rips (+1.05% SPY) on a macro catalyst (ISM Services beat) that the model explicitly identified as two-sided and refused to pre-score.
CURRENT_BEHAVIOR: When S0=S1=S2=S3=S4=0 and futures are flat, the model emits flat/flat with multiplier 0.9, treating "no fresh catalyst" as symmetric evidence against both up and down. The model identifies an upcoming macro print (ISM Services 10:00) as "two-sided" and refuses to pre-score it, but then also fails to account for the asymmetry that a beat would lift the whole market (including XLC's mega-cap growth book) while a miss would be cushioned by XLC's duration/growth character and the prior day's relative strength.
CORRECTED_BEHAVIOR: When the model has explicitly identified a same-day macro catalyst (ISM Services, NFP, CPI) as "two-sided" but the sector is a mega-cap growth/duration book with (a) prior-day relative strength ≥ +0.9%, (b) real yields not rising same-morning, and (c) no fresh sector-specific negative — the flat call should carry a directional tilt toward up/mild (not flat/flat) because the asymmetry favors upside: a macro beat lifts mega-cap growth participation, while a macro miss is partially cushioned by duration relief (falling yields help the long-duration book). The "two-sided" framing is only symmetric for the SPX; for XLC specifically, the duration/growth composition makes the ISM outcome positively skewed (beat = risk-on participation, miss = duration relief). Additionally, when the model refuses to pre-score a macro print, it should still apply a small positive S0 tilt (e.g., +0.5 to +1) for a mega-cap growth sector with prior-day relative strength, rather than forcing exactly zero.
EVIDENCE: 2026-09-03: predicted flat/flat (total 0.0), actual XLC +0.85% / SPY +1.05% / rel −0.19%. The model explicitly noted "ISM Services 10:00 ET (cons ~54.2 vs 54.1)" and "Soft labor/services are two-sided for this book (duration relief vs growth scare)" but scored S0=0. The actual ISM Services printed ~55.4 (beat), SPY ripped +1.05%, and XLC gained +0.85% — a flat call missed both direction and magnitude. The model's own Channel 2 text identified the asymmetry risk ("duration relief vs growth scare") but the scoring rubric forced a symmetric zero.
LESSON_MATCH_CHECK: Matches 2026-09-03_sector_basic_materials_lesson.md pattern (follow-through after >+1% relative day with flat futures) but is the inverse failure mode: Basic Materials lesson was about over-scoring up after a reversal day; this lesson is about under-scoring up when the model itself flags a macro catalyst as two-sided but the sector composition (mega-cap duration/growth) makes the outcome positively skewed. Also related to 08-14 lesson (follow-through after outsized relative day is usually mild/flat) — but that lesson was applied too rigidly: it capped magnitude without considering that a macro beat would provide the "new comparable catalyst" the 08-14 lesson requires for follow-through.
BACKWARD_CHECK: 2026-08-28 (down/mild predicted, XLC +1.42% actual): the model had S0=S1=0 but predicted down from stale 1w lag — this lesson would not have helped (different error: stale negative inputs). 2026-08-27 (up/flat predicted, XLC −1.07% actual): the model scored S0=+1 from AVGO/XLK AI beta — this lesson would not have caused that error (that was foreign-beta misattribution). 2026-08-26 (flat/flat predicted, XLC −0.50% actual): no macro catalyst identified as two-sided; this lesson would not apply. 2026-08-21 (down/flat predicted, XLC +0.65% actual): the model had a reversal checklist that failed (futures not ≥+0.3%) and defaulted to down — this lesson would not have flipped it to up. No backward contradiction found.
CONFLICT_CHECK: Conflicts with 08-14 lesson ("follow-through after an outsized relative day is usually mild/flat without a new comparable top-holding catalyst") — but resolves by adding the qualifier: a same-day macro catalyst (ISM, NFP) that the model itself flags as potentially market-moving IS a "new comparable catalyst" if it is scheduled and the sector is macro-sensitive. The 08-14 lesson should apply only when no scheduled macro catalyst exists. Also conflicts with the general "don't pre-score Friday NFP" rule — but that rule is about not scoring events outside the session; ISM Services at 10:00 ET is INSIDE the session and should be scored as a conditional (positive tilt for mega-cap growth if beat is more likely given prior data).
FALSIFIER: A future session where: (a) prior-day XLC relative gain ≥ +0.9%, (b) same-day ISM Services or NFP scheduled, (c) futures flat, (d) model applies +0.5 to +1 S0 tilt for mega-cap growth — but the macro print misses badly, SPY sells off >1%, and XLC falls >0.5% (duration relief fails to cushion because the growth scare dominates). If this happens, the directional tilt should be reverted to symmetric zero.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: 08-14 follow-through lesson was applied but too rigidly — it capped magnitude at mild/flat without checking whether a scheduled macro catalyst could provide the "new comparable catalyst" condition. The lesson should be amended: "follow-through after an outsized relative day is usually mild/flat UNLESS a same-day macro catalyst (ISM, NFP, CPI) is scheduled that could move the whole market; in that case, score a conditional tilt based on sector composition (mega-cap growth = positive skew on macro beat)."
SECTOR: Communication Services
LESSON_END
