---
trigger_pattern: "A pure-duration sector card (bond-proxy REITs/utilities) whose load-bearing S0/S1 inputs are a *rates* object — a stress-zone long end (30Y ≥ ~5.15%, "20-year highs") plus hawkish Fed-path language — where the cited 1d rate step is a *prior* session's close (explicitly noted as such in the card) and the live session's long end is little changed / stabilizing, while ES/NQ are positive and inside ±0.5%. The card scores the rate object as a signed negative in S0 (regime/skew) AND again as a signed negative in S1 (spine), and overrides its own 09-22 flat-cap constraint on the grounds that the spine is "signed and live.""
current_behavior: "Score the stale rate impulse at full weight — S0 = −1 (regime) + S1 rate spine = −2 (spine) = −3 for one rate object — and publish down/mild, explicitly identifying the 09-22 flat-cap (ES/NQ inside ±0.5%) as "a live constraint on the band" and then declining to apply it. Set DIVERGENCE_FLAGGED: false while the pipeline's own divergence flag is True."
corrected_behavior: "(1) A stress-zone long end is a LEVEL, not an IMPULSE. Score the level once, in the regime (S0); score the impulse in S1 only if there is a *fresh 1d step* in the rate column. When the card's own text says the 1d column is the prior session's close and the headlines are one day stale, the impulse is stale — cap the combined rate score at ~−1.5, not −3. (2) The 09-22 flat-cap binds whenever ES/NQ are inside ±0.5%, regardless of how signed the spine looks; a stale impulse does not license a mild band on a pure-duration sector. (3) When the fresh input (S4 tape: uniform multi-horizon relative lag) and the stale input (S0/S1 rate narrative) point the same way, the stale input gets undeserved credit — weight the fresh input and shrink the stale one. (4) If the pipeline flags divergence and the card's own prose names a live tension (green ES/NQ vs a strongly negative leading sum), set DIVERGENCE_FLAGGED: true and let it cap the band."
evidence_cited: "Predicted down/mild, total_score −4.619, S0 −1 / S1 −3, DIVERGENCE_FLAGGED false. Actual XLRE −0.216% / SPY +0.544% / rel −0.760% — direction HIT, magnitude MISS (flat). CNBC same-session: "10-year Treasury yield is little changed to end a volatile week" — the fresh break did not extend. Card's own text: "That 1d column is Wednesday's close, not this open" and "Today ES/NQ are inside ±0.5% (positive), so the 09-22 flat-cap is a live constraint on the band" — constraint named, then overridden. Intraday shape was fade-from-a-green-open (rotation signature), not gap-down-and-hold (shock signature). Pipeline divergence_flagged: True vs overlay DIVERGENCE_FLAGGED: false."
error_category: "A"
falsifier: "If a future pure-duration sector card cites a *prior-session* 1d rate step, notes the live long end is little changed, has ES/NQ positive inside ±0.5%, and the sector still realizes a notable absolute loss (|XLRE| ≥ ~0.75%) driven by a fresh same-session rate extension — then impulse staleness did not cap the band and this lesson is wrong. Conversely, if a card with a *fresh* 1d rate step and ES/NQ inside ±0.5% is capped to flat and the sector realizes notable, the flat-cap half of this lesson is wrong."
sector: "Real Estate"
date: "2026-09-25"
status: "candidate"
---

# Sector Reflection — Real Estate — 2026-09-25

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A pure-duration sector card (bond-proxy REITs/utilities) whose load-bearing S0/S1 inputs are a *rates* object — a stress-zone long end (30Y ≥ ~5.15%, "20-year highs") plus hawkish Fed-path language — where the cited 1d rate step is a *prior* session's close (explicitly noted as such in the card) and the live session's long end is little changed / stabilizing, while ES/NQ are positive and inside ±0.5%. The card scores the rate object as a signed negative in S0 (regime/skew) AND again as a signed negative in S1 (spine), and overrides its own 09-22 flat-cap constraint on the grounds that the spine is "signed and live."
CURRENT_BEHAVIOR: Score the stale rate impulse at full weight — S0 = −1 (regime) + S1 rate spine = −2 (spine) = −3 for one rate object — and publish down/mild, explicitly identifying the 09-22 flat-cap (ES/NQ inside ±0.5%) as "a live constraint on the band" and then declining to apply it. Set DIVERGENCE_FLAGGED: false while the pipeline's own divergence flag is True.
CORRECTED_BEHAVIOR: (1) A stress-zone long end is a LEVEL, not an IMPULSE. Score the level once, in the regime (S0); score the impulse in S1 only if there is a *fresh 1d step* in the rate column. When the card's own text says the 1d column is the prior session's close and the headlines are one day stale, the impulse is stale — cap the combined rate score at ~−1.5, not −3. (2) The 09-22 flat-cap binds whenever ES/NQ are inside ±0.5%, regardless of how signed the spine looks; a stale impulse does not license a mild band on a pure-duration sector. (3) When the fresh input (S4 tape: uniform multi-horizon relative lag) and the stale input (S0/S1 rate narrative) point the same way, the stale input gets undeserved credit — weight the fresh input and shrink the stale one. (4) If the pipeline flags divergence and the card's own prose names a live tension (green ES/NQ vs a strongly negative leading sum), set DIVERGENCE_FLAGGED: true and let it cap the band.
EVIDENCE: Predicted down/mild, total_score −4.619, S0 −1 / S1 −3, DIVERGENCE_FLAGGED false. Actual XLRE −0.216% / SPY +0.544% / rel −0.760% — direction HIT, magnitude MISS (flat). CNBC same-session: "10-year Treasury yield is little changed to end a volatile week" — the fresh break did not extend. Card's own text: "That 1d column is Wednesday's close, not this open" and "Today ES/NQ are inside ±0.5% (positive), so the 09-22 flat-cap is a live constraint on the band" — constraint named, then overridden. Intraday shape was fade-from-a-green-open (rotation signature), not gap-down-and-hold (shock signature). Pipeline divergence_flagged: True vs overlay DIVERGENCE_FLAGGED: false.
LESSON_MATCH_CHECK: Matches the 2026-09-25_sector_financial_lesson candidate (rates object scored as signed negative in S0 then re-scored as signed negative in S1 via a nested HEAT book, on a GREEN agreeing tape) — same double-count-of-one-rate-object structure, different sector. Also matches 2026-09-25_sector_consumer_defensive_lesson (rates object load-bearing while live equity tape is green) and 2026-09-25_sector_communication_services_lesson (own factor sum clearly negative but band not paid). Distinct from 2026-09-24_sector_real_estate_lesson (a clean hit with a *live* signed hawkish S0) — the discriminator is impulse freshness, not sign.
BACKWARD_CHECK: Re-running 09-25 with the corrected rule: S0 = −0.5 (level, scored once), S1 = −1 (stale impulse, no fresh 1d step) + −0.5 (office/mortgage/refi structural, already paid in the −8.08% 1m rel) = −1.5, S2 = −1, S3 = −0.5, S4 = −1 ⇒ leading sum ≈ −4.0, ×0.85 ≈ −3.4, band capped at flat by the 09-22 constraint ⇒ down/flat. Actual −0.216% = flat ⇒ magnitude HIT. The rule converts a mag MISS into a mag HIT without changing the direction. It also does not break 09-23 (down/mild, actual −1.761% notable): there the long end was in a *fresh* stress-zone rip with a live 1d step, so the impulse was live and the band legitimately expanded. And it does not break 09-24 (down/mild, actual −0.454%, mag HIT): that card had a live signed hawkish S0 with confirming multi-horizon lag.
CONFLICT_CHECK: No conflict with active lessons. 08-27 ("30Y in stress zone ⇒ cap S0/S1 at 0") is *reinforced*, not contradicted — the card invoked the 08-17/08-18 smash branch to supersede the cap, but the smash branch requires a *live* impulse; with a stale impulse the cap's spirit applies. 09-23 (stress-zone skew may expand the band) is preserved but bounded: expansion requires a fresh 1d rate step. 09-22 (unsigned S0 + rotation-out + ES/NQ inside ±0.5% ⇒ down/flat) is *strengthened* — the card named it and overrode it; this lesson makes the override impermissible. 09-24 (verify the live curve independently; don't let one name define the call) is honored — the card did verify independently and did not let WELL/EQIX drive the call; that discipline held.
FALSIFIER: If a future pure-duration sector card cites a *prior-session* 1d rate step, notes the live long end is little changed, has ES/NQ positive inside ±0.5%, and the sector still realizes a notable absolute loss (|XLRE| ≥ ~0.75%) driven by a fresh same-session rate extension — then impulse staleness did not cap the band and this lesson is wrong. Conversely, if a card with a *fresh* 1d rate step and ES/NQ inside ±0.5% is capped to flat and the sector realizes notable, the flat-cap half of this lesson is wrong.
DIVERGENCE_VERDICT: futures_right — the pipeline's divergence_flagged: True was correct; the LLM overlay's DIVERGENCE_FLAGGED: false was the error. The green ES/NQ (+0.28%/+0.57%) against a −5.5 leading sum was the day's actual driver (index risk-on pulling capital from bond proxies), not merely "XLK/AI beta" to be dismissed. The overlay's dismissal was half-right (not REIT duration relief) and half-wrong (it *was* the marginal driver).
ACTIVE_LESSON_REVIEW: 08-27 (cap S0/S1 at 0 when 30Y in stress zone) — should have bound harder; the smash-branch override required a live impulse that did not exist. 08-25 (verify live curve independently; Finviz board is not a live read) — correctly applied. 08-17/08-18 smash branch — invoked but precondition (live long-end rip) was stale, not live; branch should have been OFF. 09-08 cushion — correctly did not fire. 09-11 no-force-down — precondition not met, correctly handled. 09-14 (XLRE absent from PM board ⇒ scored 0, no sign/offset) — correctly applied. 09-15 flatten-mag — correctly not applied (fresh, not telegraphed). 09-21 (every-horizon relative lag is the MACRO MAP object) — correctly identified but under-weighted relative to the daily rate narrative; the −8.08% 1m rel was the better predictor. 09-22 flat-cap — named and then overridden; this is the pivotal error. 09-23 skew — applied one day late to a stale setup. 09-24 (independent curve verification; no single-name driver) — correctly applied. New binding rule added: impulse freshness gate on the rate object.
SECTOR: Real Estate
LESSON_END
