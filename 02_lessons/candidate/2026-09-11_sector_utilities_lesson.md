---
trigger_pattern: "A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open and the sector has a KNOWN ASYMMETRIC beta to the two branches (e.g., a bond-proxy/duration sector whose downside beta to a hot print is materially larger than its upside beta to a soft print). The model correctly identifies the asymmetry in prose ("mixed-to-mildly-negative for a bond proxy") but then scores the shared-macro component at exactly 0, citing "do not pre-score either branch.""
current_behavior: "The model applies the 09-03/09-04 rule ("do not pre-score either branch of a two-sided binary") as if it mandated a zero score for S0. It writes the asymmetry out loud, then discards it, producing an internal contradiction between prose (mildly negative) and score (0). The same over-application also mislabels a same-day-catalyzed factor as "carried" in S1 and declines to double-count it, so a single live directional chain (CPI → hike odds → long end → duration) gets scored roughly once at half weight instead of once at full weight — a double-discount rather than a double-count."
corrected_behavior: ""Do not pre-score either branch" means do not commit to an outcome; it does NOT mean score the expected value at zero. When a live binary has asymmetric sector beta, score the asymmetry: S0 takes the sign of the larger-beta branch even at ~50/50 odds. Separately, a factor with a same-day catalyst is LIVE, not "carried" — pre-positioning (e.g., 1m DGS10 +11bp) is the setup, the release is the trigger; score it at full weight in S1 and trust the HIT_GRID over the narrative label. Do not let fear of double-counting produce a double-discount of the one chain that matters."
evidence_cited: "2026-09-11 Utilities. Morning prose: "the macro is mixed-to-mildly-negative for a bond proxy" but S0 = 0. Morning S1 labeled rates-rising "PARTIAL / carried" and explicitly said "do not HIT and do not double-count with S0," while the same-run HIT_GRID scored "Rates rising (bond-proxy selloff)" as HIT at 0.60 and "Risk-on rotation away" as PARTIAL at 0.50 — grid and score disagree. Realized: XLU −0.31% absolute, SPY +0.85%, rel −1.16% (≈3× the prior-day rel fade of −0.38%). Hot August CPI (0.4% m/m, 3.4% y/y) lifted hike odds and repriced the long end; SPY took the same print as growth-tolerant risk-on. Direction HIT, magnitude MISS on the relative dimension. Post-session review independently reached the same conclusion: S0 should have been −1, S1 −2, S4 −1."
error_category: "A"
falsifier: "This lesson is falsified if, on a future scheduled-binary day with a duration-sensitive sector, the model scores S0 negative on asymmetric-beta grounds and the sector instead OUTPERFORMS (rel > 0) because the soft-CPI branch resolved — i.e., the asymmetry was not real or was swamped by a sector-specific offset. It is also falsified if scoring a same-day-catalyzed factor at full weight in S1 (rather than "carried") systematically produces double-counting errors on days when the pre-positioned move and the catalyst are genuinely the same observation. Concretely: if a future CPI-day Utilities call with S0 = −1 and S1 = −2 resolves as a direction MISS to the upside, the asymmetry premise is wrong for this sector."
sector: "Utilities"
date: "2026-09-11"
status: "promoted"
---

# Sector Reflection — Utilities — 2026-09-11

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open and the sector has a KNOWN ASYMMETRIC beta to the two branches (e.g., a bond-proxy/duration sector whose downside beta to a hot print is materially larger than its upside beta to a soft print). The model correctly identifies the asymmetry in prose ("mixed-to-mildly-negative for a bond proxy") but then scores the shared-macro component at exactly 0, citing "do not pre-score either branch."
CURRENT_BEHAVIOR: The model applies the 09-03/09-04 rule ("do not pre-score either branch of a two-sided binary") as if it mandated a zero score for S0. It writes the asymmetry out loud, then discards it, producing an internal contradiction between prose (mildly negative) and score (0). The same over-application also mislabels a same-day-catalyzed factor as "carried" in S1 and declines to double-count it, so a single live directional chain (CPI → hike odds → long end → duration) gets scored roughly once at half weight instead of once at full weight — a double-discount rather than a double-count.
CORRECTED_BEHAVIOR: "Do not pre-score either branch" means do not commit to an outcome; it does NOT mean score the expected value at zero. When a live binary has asymmetric sector beta, score the asymmetry: S0 takes the sign of the larger-beta branch even at ~50/50 odds. Separately, a factor with a same-day catalyst is LIVE, not "carried" — pre-positioning (e.g., 1m DGS10 +11bp) is the setup, the release is the trigger; score it at full weight in S1 and trust the HIT_GRID over the narrative label. Do not let fear of double-counting produce a double-discount of the one chain that matters.
EVIDENCE: 2026-09-11 Utilities. Morning prose: "the macro is mixed-to-mildly-negative for a bond proxy" but S0 = 0. Morning S1 labeled rates-rising "PARTIAL / carried" and explicitly said "do not HIT and do not double-count with S0," while the same-run HIT_GRID scored "Rates rising (bond-proxy selloff)" as HIT at 0.60 and "Risk-on rotation away" as PARTIAL at 0.50 — grid and score disagree. Realized: XLU −0.31% absolute, SPY +0.85%, rel −1.16% (≈3× the prior-day rel fade of −0.38%). Hot August CPI (0.4% m/m, 3.4% y/y) lifted hike odds and repriced the long end; SPY took the same print as growth-tolerant risk-on. Direction HIT, magnitude MISS on the relative dimension. Post-session review independently reached the same conclusion: S0 should have been −1, S1 −2, S4 −1.
LESSON_MATCH_CHECK: Strong match to the 2026-09-11_sector_real_estate_lesson.md candidate ("pre-open tape unambiguously one-sided... yet the model scores S0 = 0 on the grounds that [the binary is unresolved]") — same failure mode in a sibling bond-proxy sector on the same day. Also matches 2026-09-11_sector_consumer_defensive_lesson.md ("names a factor as a [carried/neutral]...") and the family of 09-11 sector lessons (energy, financial, technology, basic_materials) all describing the same CPI-day S0-zeroing / band-flattening pathology. The 09-10 Utilities lesson (positive medium-term rel tape + 08-18 frame) is a different trigger and does not match. This is a recurring, cross-sector pattern, not a one-off.
BACKWARD_CHECK: Re-running 2026-09-11 with S0 = −1 (asymmetric duration beta), S1 = −2 (rates-rising live HIT + rotation-away full HIT), S4 = −1 (prior-day rel fade into a duration-sensitive binary) gives leading_sum ≈ −4.0, ×0.9 ≈ −3.6 to −4.0 → down/notable, versus the emitted down/flat. That is closer to the realized rel −1.16% and would have converted the magnitude call from MISS toward HIT on the relative dimension. The correction is falsifiable and improves the graded outcome on this instance. It does not disturb the direction call (already HIT) and does not require re-scoring any other 09-11 sector whose beta to CPI was symmetric or positive.
CONFLICT_CHECK: Potential conflict with the 09-03/09-04 rule as literally worded ("do not pre-score either branch"). Resolved by scope: that rule governs committing to a directional OUTCOME, not the expected-value SIGN of an asymmetric exposure. No conflict with the 09-10 gate (VIX <20 + backwardated → rising long end is a relative-lag signal) — that gate is reinforced and should be promoted to a standing rule. No conflict with 08-25 ("S0=S1=0 → do not manufacture down from carried lag") — here S1 was not 0 and the factor was live, not carried. No conflict with 08-28 (single-name items must not drive the ETF) — no single name was promoted. No conflict with 08-12 (AI-power is a 1d dampener) — structural positives were correctly held stale.
FALSIFIER: This lesson is falsified if, on a future scheduled-binary day with a duration-sensitive sector, the model scores S0 negative on asymmetric-beta grounds and the sector instead OUTPERFORMS (rel > 0) because the soft-CPI branch resolved — i.e., the asymmetry was not real or was swamped by a sector-specific offset. It is also falsified if scoring a same-day-catalyzed factor at full weight in S1 (rather than "carried") systematically produces double-counting errors on days when the pre-positioned move and the catalyst are genuinely the same observation. Concretely: if a future CPI-day Utilities call with S0 = −1 and S1 = −2 resolves as a direction MISS to the upside, the asymmetry premise is wrong for this sector.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: The 09-10 gate ("VIX <20 and backwardated → a rising long end is a relative-LAG signal for XLU, not a relative cushion") performed exactly as written and predicted the realized failure mode (relative lag, not absolute collapse) — promote from lesson to standing rule; do not weaken it. The 09-03/09-04 "do not call flat into a high-impact binary" rule was correctly invoked to widen the band in prose but was then contradicted by the pipeline emitting `predicted_magnitude_band: flat` — the band must be inherited from the prose, not recomputed narrower; this is a pipeline bug, not a reasoning bug, and should be logged separately. The 08-25 rule ("S0=S1=0 → do not manufacture down") did not bind here and was correctly not invoked. The 08-12 rule (AI-power = 1d dampener, not band engine) held up and should be retained. The 08-28 rule (no single-name promotion) held up. Net: one new active lesson (asymmetric-binary S0 scoring + "carried ≠ not live"), one promotion (09-10 gate → standing rule), one pipeline fix flagged (band inheritance).
SECTOR: Utilities
LESSON_END
