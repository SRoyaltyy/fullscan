---
trigger_pattern: "A low-beta defensive sector ETF (XLV/XLP/XLU-like) on a strong risk-on session (index futures ≥ +1%, growth/tech leading by a wide margin, oil offered, VIX contango) where the sector's own live premarket print is red-to-flat (PM ≤ 0) against a leading peer (PM:XLK ≥ +0.5%). The card correctly identifies the funding-source/rotation-out dynamic and scores S0 negative, but collapses RELATIVE underperformance and ABSOLUTE decline into a single negative S0 — then the v2 engine's index_carry leg (large positive, e.g. +3.2) plus tape_anchor arithmetically overrides the negative leading sum and mints up/mild. Actual outcome: mild up absolute, notable down relative."
current_behavior: "The card scores S0_SHARED_MACRO = −0.5 to −3.5 on a "funding source" thesis, treating relative rotation-out as if it implied absolute decline. The engine then fights the negative leading sum with index_carry and prints up/mild for a non-sector reason (ES beta carry), while the LLM overlay reinforces the funding-source-implies-down error. Neither side models the correct decomposition: absolute = beta × tape (mild up), relative = rotation drag (notable down). The divergence flag fires but the system has no mechanism to reconcile the two half-right models."
corrected_behavior: "When a low-beta defensive is the funding source on a strong up tape, decompose explicitly: (1) ABSOLUTE = sector beta (~0.6–0.7) × index tape (e.g. +1.55%) ≈ +0.9–1.1% gross lift; (2) RELATIVE = rotation drag (~−1.0 to −1.5%); (3) NET absolute ≈ flat-to-mild-up. Score S0 as a small negative (≈ −0.2 to −0.3) for the relative drag, NOT a large negative implying absolute decline. Route the funding-source effect to a relative/RS channel rather than the absolute S0. Run the beta-arithmetic sanity check at the S0 stage before finalizing direction. When the engine's index_carry and the overlay's funding-source logic disagree, the correct resolution is "up mild absolute / down notable relative" — not "trust factors over tape" (which would produce a wrong flat/down absolute call)."
evidence_cited: "2026-09-21 Healthcare/XLV: predicted up/mild (engine, total_score 3.016, index_carry 3.218, tape_anchor 0.198, llm_overlay −0.4, divergence_flagged True); actual +0.35% absolute (up/mild — direction and magnitude HIT), rel −1.20% (notable relative lag). Morning S0 = −0.5 implied absolute weakness; reality was mild up absolute. The 09-18 complement rule ("paid FOMC + PM ≤ 0 → do not accept ES tape_anchor up/mild") fired and was WRONG today — up/mild is exactly what printed. The 08-13 reversal-tell ban on up/notable was correct (magnitude mild). Ex-div ~0.38% mechanical drag correctly excluded from scoring. Single-name discipline (NVO/LLY/AMGN/ARGX/BSX) held correctly."
error_category: "C"
falsifier: "If a future session shows a low-beta defensive with PM ≤ 0 on a strong risk-on tape (SPY ≥ +1%) and the sector closes DOWN absolute (not just lagging relative), then the beta-arithmetic decomposition is wrong and the funding-source effect CAN flip absolute direction — invalidating this lesson. Conversely, if a modest-tape session (SPY < +0.75%) with PM ≤ 0 produces a mild-up absolute close, the tape-strength qualifier is too strict."
sector: "Healthcare"
date: "2026-09-21"
status: "promoted"
---

# Sector Reflection — Healthcare — 2026-09-21

LESSON_BEGIN
ERROR_CATEGORY: C
TRIGGER_PATTERN: A low-beta defensive sector ETF (XLV/XLP/XLU-like) on a strong risk-on session (index futures ≥ +1%, growth/tech leading by a wide margin, oil offered, VIX contango) where the sector's own live premarket print is red-to-flat (PM ≤ 0) against a leading peer (PM:XLK ≥ +0.5%). The card correctly identifies the funding-source/rotation-out dynamic and scores S0 negative, but collapses RELATIVE underperformance and ABSOLUTE decline into a single negative S0 — then the v2 engine's index_carry leg (large positive, e.g. +3.2) plus tape_anchor arithmetically overrides the negative leading sum and mints up/mild. Actual outcome: mild up absolute, notable down relative.
CURRENT_BEHAVIOR: The card scores S0_SHARED_MACRO = −0.5 to −3.5 on a "funding source" thesis, treating relative rotation-out as if it implied absolute decline. The engine then fights the negative leading sum with index_carry and prints up/mild for a non-sector reason (ES beta carry), while the LLM overlay reinforces the funding-source-implies-down error. Neither side models the correct decomposition: absolute = beta × tape (mild up), relative = rotation drag (notable down). The divergence flag fires but the system has no mechanism to reconcile the two half-right models.
CORRECTED_BEHAVIOR: When a low-beta defensive is the funding source on a strong up tape, decompose explicitly: (1) ABSOLUTE = sector beta (~0.6–0.7) × index tape (e.g. +1.55%) ≈ +0.9–1.1% gross lift; (2) RELATIVE = rotation drag (~−1.0 to −1.5%); (3) NET absolute ≈ flat-to-mild-up. Score S0 as a small negative (≈ −0.2 to −0.3) for the relative drag, NOT a large negative implying absolute decline. Route the funding-source effect to a relative/RS channel rather than the absolute S0. Run the beta-arithmetic sanity check at the S0 stage before finalizing direction. When the engine's index_carry and the overlay's funding-source logic disagree, the correct resolution is "up mild absolute / down notable relative" — not "trust factors over tape" (which would produce a wrong flat/down absolute call).
EVIDENCE: 2026-09-21 Healthcare/XLV: predicted up/mild (engine, total_score 3.016, index_carry 3.218, tape_anchor 0.198, llm_overlay −0.4, divergence_flagged True); actual +0.35% absolute (up/mild — direction and magnitude HIT), rel −1.20% (notable relative lag). Morning S0 = −0.5 implied absolute weakness; reality was mild up absolute. The 09-18 complement rule ("paid FOMC + PM ≤ 0 → do not accept ES tape_anchor up/mild") fired and was WRONG today — up/mild is exactly what printed. The 08-13 reversal-tell ban on up/notable was correct (magnitude mild). Ex-div ~0.38% mechanical drag correctly excluded from scoring. Single-name discipline (NVO/LLY/AMGN/ARGX/BSX) held correctly.
LESSON_MATCH_CHECK: Matches 2026-09-21_sector_consumer_defensive_lesson.md (low-beta defensive XLP/XLU/XLV-like, net-negative leading card, strong risk-on, engine index_carry flattens negative leading sum into flat/flat while LLM says down) — but that lesson's outcome was flat/flat official vs down LLM; here the engine printed up/mild and was RIGHT on absolute. Also matches 2026-09-18_sector_healthcare_lesson.md (same sector, same post-paid-FOMC + PM ≤ 0 + tech-led risk-on + tape_anchor/index_carry writing up/mild) — but 09-18 closed −0.25% (dir MISS) while 09-21 closed +0.35% (dir HIT). The distinguishing variable is tape STRENGTH: 09-18 was a modest green tape; 09-21 was SPY +1.55%. This is the tape-strength qualifier the 09-18 rule lacked.
BACKWARD_CHECK: Would this lesson have improved the 09-21 call? Yes — the beta-arithmetic check (SPY +1.35–1.55% × XLV beta ~0.65 ≈ +0.9–1.0% gross, minus ~−1.2% rotation drag ≈ flat-to-mild-up) would have converted the morning's flat/down lean into the correct up/mild absolute call while preserving the correct relative-lag call. Would it have hurt 09-18? No — on 09-18 the tape was modestly green (not +1.5%), so beta lift would have been smaller and the funding-source drag could plausibly net negative; the tape-strength qualifier distinguishes the two sessions. Would it have hurt 09-15/09-16 (up/mild predicted, actual ~flat, dir MISS)? Possibly — those were also modest tapes, so the qualifier would have kept conviction low. Net: the lesson is additive, not destructive.
CONFLICT_CHECK: Conflicts with the 09-18 complement rule as written ("paid FOMC + PM ≤ 0 → do not accept ES tape_anchor up/mild"). That rule fired on 09-21 and was wrong. Resolution: add a tape-strength qualifier — the rule should only suppress the tape_anchor when the index tape is MODESTLY green (e.g. ES/SPY < +0.75%), not when SPY is on track for +1.5%. Also conflicts with the general "trust factors over tape" heuristic when factors are net-negative but the tape is strongly directional — in that case the factors are measuring RELATIVE drag, not ABSOLUTE direction, and the tape owns absolute. No conflict with the 08-13 reversal-tell ban (magnitude was mild, ban held) or the ex-div/single-name rules (both held).
FALSIFIER: If a future session shows a low-beta defensive with PM ≤ 0 on a strong risk-on tape (SPY ≥ +1%) and the sector closes DOWN absolute (not just lagging relative), then the beta-arithmetic decomposition is wrong and the funding-source effect CAN flip absolute direction — invalidating this lesson. Conversely, if a modest-tape session (SPY < +0.75%) with PM ≤ 0 produces a mild-up absolute close, the tape-strength qualifier is too strict.
DIVERGENCE_VERDICT: futures_right — the engine's up/mild (riding index_carry/ES beta) was correct on absolute direction and magnitude; the LLM overlay's funding-source logic was correct on relative but wrong on absolute. The divergence flag correctly surfaced the split but the system could not reconcile the two half-right models into "up mild absolute / down notable relative."
ACTIVE_LESSON_REVIEW: The 09-18 complement rule needs amendment (tape-strength qualifier). The 08-13 reversal-tell ban on up/notable held correctly. The 08-28 leftover-RS ban held correctly (1w RS not copied into S2/S3/S4). The 09-10 same-shock rule held (oil-offered + NQ-green + 1d real-yield dip scored once in S0). The 09-11 funding-source rule fired correctly on the relative call but was over-weighted into absolute. The 09-14 destination rule correctly did not fire (NQ leading up, not down). The 09-15 S0~0 complement correctly did not fire (tech-led risk-on, not uniform risk-off). The 09-16 engine-up/force-flat correctly did not fire (FOMC paid). The 09-17 keep-up correctly did not fire (PM:XLV −0.30%, not ~+0.4%). Ex-div handling textbook. Single-name discipline excellent.
SECTOR: Healthcare
LESSON_END
