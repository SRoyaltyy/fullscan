---
trigger_pattern: "Sector ETF prediction where the model identifies a same-day two-sided macro catalyst (ISM Services) and refuses to pre-score it, futures are flat (ES +0.13% / NQ +0.16%), and the sector has no fresh same-morning catalyst — the model scores all factors zero and predicts flat/flat, but the actual session delivers a broad-market rip (+1.05% SPY) on the macro catalyst beat, producing a large absolute move in the sector ETF that the flat band misses entirely."
current_behavior: "When a high-impact macro print (ISM Services) is identified as two-sided and explicitly refused pre-scoring, the model treats "unscored" as "zero contribution" and allows the flat prediction to stand, even though the calendar event is the single largest source of session variance. The model also applies the follow-through rule asymmetrically: it uses prior-day relative moves to cap magnitude at flat but does not consider that a flat-futures morning with a pending macro catalyst can resolve violently in either direction."
corrected_behavior: "When a high-impact macro print is pending at the open and identified as two-sided, the model must widen the magnitude band to at least mild (not flat) regardless of component scores, because the unresolved binary is itself a source of variance that flat cannot capture. Additionally, when the model identifies a same-day macro catalyst as the dominant session driver, it should treat the sector's prior relative performance as context for direction but not as a magnitude cap — a broad-market rip on a macro beat can produce +1% absolute moves in sectors with no fresh catalyst, and flat predictions on such mornings should be reserved for sessions where no high-impact print is pending."
evidence_cited: "2026-09-03 Industrials: predicted flat/flat (total_score 0.0, confidence 0.52) with ISM Services explicitly identified as "two-sided, not scored until print" and "do not pre-score a miss or a beat." Actual: XLI +1.03% absolute, SPY +1.05%, ISM Services beat drove a broad-market rip. The model's own text noted "ISM Services is not the manufacturing spine" and "Encode as event risk in S0" — but encoding as event risk with S0=0 meant the event contributed nothing to the score, and the flat band had no mechanism to capture the macro-driven upside. The 09-03 sector lessons for Basic Materials, Communication Services, Consumer Cyclical, Consumer Defensive, Energy, Financial, and Healthcare all document the same failure mode across sectors on the same day."
error_category: "B"
falsifier: "A session where a high-impact macro print is pending at the open, the model predicts flat/flat, the print comes in as expected (not a beat/miss), and the sector moves <0.5% — this would show that pending macro does not always require a wider band. However, the asymmetric risk (a beat or miss can produce ±1% moves while an in-line print produces small moves) means the expected variance is still higher than a no-print session, so widening to mild remains the safer default. A second falsifier: a session where the model widens to mild on pending macro, the print is in-line, and the sector moves <0.3% — this would show the widening was unnecessary, but it would be a cost of insurance, not a model error."
sector: "Industrials"
date: "2026-09-03"
status: "promoted"
---

# Sector Reflection — Industrials — 2026-09-03

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: Sector ETF prediction where the model identifies a same-day two-sided macro catalyst (ISM Services) and refuses to pre-score it, futures are flat (ES +0.13% / NQ +0.16%), and the sector has no fresh same-morning catalyst — the model scores all factors zero and predicts flat/flat, but the actual session delivers a broad-market rip (+1.05% SPY) on the macro catalyst beat, producing a large absolute move in the sector ETF that the flat band misses entirely.
CURRENT_BEHAVIOR: When a high-impact macro print (ISM Services) is identified as two-sided and explicitly refused pre-scoring, the model treats "unscored" as "zero contribution" and allows the flat prediction to stand, even though the calendar event is the single largest source of session variance. The model also applies the follow-through rule asymmetrically: it uses prior-day relative moves to cap magnitude at flat but does not consider that a flat-futures morning with a pending macro catalyst can resolve violently in either direction.
CORRECTED_BEHAVIOR: When a high-impact macro print is pending at the open and identified as two-sided, the model must widen the magnitude band to at least mild (not flat) regardless of component scores, because the unresolved binary is itself a source of variance that flat cannot capture. Additionally, when the model identifies a same-day macro catalyst as the dominant session driver, it should treat the sector's prior relative performance as context for direction but not as a magnitude cap — a broad-market rip on a macro beat can produce +1% absolute moves in sectors with no fresh catalyst, and flat predictions on such mornings should be reserved for sessions where no high-impact print is pending.
EVIDENCE: 2026-09-03 Industrials: predicted flat/flat (total_score 0.0, confidence 0.52) with ISM Services explicitly identified as "two-sided, not scored until print" and "do not pre-score a miss or a beat." Actual: XLI +1.03% absolute, SPY +1.05%, ISM Services beat drove a broad-market rip. The model's own text noted "ISM Services is not the manufacturing spine" and "Encode as event risk in S0" — but encoding as event risk with S0=0 meant the event contributed nothing to the score, and the flat band had no mechanism to capture the macro-driven upside. The 09-03 sector lessons for Basic Materials, Communication Services, Consumer Cyclical, Consumer Defensive, Energy, Financial, and Healthcare all document the same failure mode across sectors on the same day.
LESSON_MATCH_CHECK: Matches 2026-09-03_sector_communication_services_lesson.md (flat-futures morning after outsized relative day, macro catalyst beat produces +0.85% absolute), 2026-09-03_sector_consumer_defensive_lesson.md (flat-futures morning, macro beat produces −1.36% relative reversal), 2026-09-03_sector_energy_lesson.md (extended relative run + flat futures + macro beat produces −1.78% relative), 2026-09-03_sector_financial_lesson.md (flat-futures morning, macro beat produces +1.56% absolute), and 2026-09-03_sector_healthcare_lesson.md (flat call directionally correct but misses relative underperformance on macro rip). This is the same systemic failure: two-sided macro catalyst + flat prediction = missed variance.
BACKWARD_CHECK: 2026-08-28 predicted down/flat, actual −0.93% (dir HIT, mag MISS on pipeline flat) — no pending high-impact macro print that morning, so widening for pending macro would not have changed this call. 2026-08-26 predicted flat/flat, actual +1.09% (dir MISS) — need to check if a high-impact print was pending; if so, this lesson would have helped. 2026-08-27 predicted up/notable, actual −0.85% (dir MISS) — this was a reversal call failure, not a macro-variance miss. The lesson does not contradict these; it addresses a specific configuration (pending high-impact macro + flat prediction) that recurs across sectors.
CONFLICT_CHECK: No conflict with 08-27 (forbid up on laggard + non-holdings AHR) — that lesson caps direction on specific conditions; this lesson widens magnitude when a high-impact macro binary is pending, which is orthogonal. No conflict with 08-13 (oil-down cap) — that lesson governs S0 scoring on stale supply headlines. No conflict with 09-02 residual rule (do not restack completed smash) — that lesson prevents double-counting prior-day moves; this lesson addresses unresolved same-day macro variance. The DO-INSTEAD rule "when score fights tape, cut conviction / prefer flat/mild" remains valid for sessions without pending high-impact macro; this lesson adds that pending high-impact macro is itself a reason to widen the band, not shrink it.
FALSIFIER: A session where a high-impact macro print is pending at the open, the model predicts flat/flat, the print comes in as expected (not a beat/miss), and the sector moves <0.5% — this would show that pending macro does not always require a wider band. However, the asymmetric risk (a beat or miss can produce ±1% moves while an in-line print produces small moves) means the expected variance is still higher than a no-print session, so widening to mild remains the safer default. A second falsifier: a session where the model widens to mild on pending macro, the print is in-line, and the sector moves <0.3% — this would show the widening was unnecessary, but it would be a cost of insurance, not a model error.
DIVERGENCE_VERDICT: none_flagged — the model correctly identified no divergence between leading factors and tape (all zeros), but the failure was in not recognizing that a pending high-impact macro catalyst is itself a source of variance that flat cannot capture, regardless of factor/tape agreement.
ACTIVE_LESSON_REVIEW: 08-27 (forbid up on laggard + non-holdings AHR) — correctly applied, but the lesson's "prefer flat or down:mild" guidance contributed to the flat call; this lesson refines that guidance by adding that pending high-impact macro overrides the flat preference. 08-25 (need fresh single-name smash to force down:mild) — correctly applied; no fresh smash existed, but the lesson's framing (only down needs a trigger) missed that up also needs no trigger when a macro beat drives the whole market. 09-02 residual rule (do not restack completed smash) — correctly applied; the issue was not restacking but failing to account for the pending macro binary.
SECTOR: Industrials
LESSON_END
