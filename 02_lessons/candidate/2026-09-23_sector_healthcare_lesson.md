---
trigger_pattern: "A sector factor card nets S0–S4 to all-zero (leading_sum = 0, divergence = false, size_gate applied) on a mixed, empty-spine session — yet the deterministic engine still emits a signed direction (up/mild) off `index_carry` / `tape_anchor` alone, where index_carry is a general-market carry signal (general_total ≫ sector total) that is not a healthcare factor. The card's own self-audit explicitly warns "trust this factor card (flat) over tape," but the engine does not honor it."
current_behavior: "When leading_sum = 0 and divergence = false, the engine's `index_carry` (0.573, derived from general_total 2.293) and `tape_anchor` (−0.01) are allowed to set `predicted_direction = up / mild` even though every sector channel scored 0. The LLM overlay was 0.0, so the signed call came purely from cross-object leakage. Result: predicted up/mild vs actual −0.124% → direction MISS, magnitude MISS."
corrected_behavior: "When leading_sum = 0 AND divergence = false AND size_gate is applied, the engine must emit **flat/flat** (unsigned), not a signed direction off index_carry. index_carry is a general-market carry term and must not drive a sector call in the absence of any sector-channel signal. Promote the card's "trust the factor card over tape" note to a hard gate: `if leading_sum == 0 and not divergence_flagged: suppress index_carry and tape_anchor from direction determination`."
evidence_cited: "Pipeline JSON: leading_sum 0.0, divergence_flagged False, all S0–S4 = 0.0, overlay_score 0.0, yet predicted_direction 'up', magnitude 'mild', index_carry 0.573, general_total 2.293. Outcome: XLV −0.124% absolute (down), +0.596% relative. Card self-audit: "If the engine tries leftover ES tape_anchor or leftover RS veto, trust this factor card (flat) over tape." Post-session review: "the engine's up/mild was wrong, the card's flat was right.""
error_category: "B"
falsifier: "This lesson is falsified if a future session has leading_sum = 0, divergence = false, size_gate applied, and the sector ETF nonetheless moves ≥±0.5% absolute in a direction predictable from index_carry/tape_anchor alone (i.e., the carry term carried genuine sector information). If such a case recurs with the gate applied and the flat call misses a ≥0.5% move, the gate is too aggressive and should be relaxed to a confidence-shrink rather than a direction suppression."
sector: "Healthcare"
date: "2026-09-23"
status: "candidate"
---

# Sector Reflection — Healthcare — 2026-09-23

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: A sector factor card nets S0–S4 to all-zero (leading_sum = 0, divergence = false, size_gate applied) on a mixed, empty-spine session — yet the deterministic engine still emits a signed direction (up/mild) off `index_carry` / `tape_anchor` alone, where index_carry is a general-market carry signal (general_total ≫ sector total) that is not a healthcare factor. The card's own self-audit explicitly warns "trust this factor card (flat) over tape," but the engine does not honor it.
CURRENT_BEHAVIOR: When leading_sum = 0 and divergence = false, the engine's `index_carry` (0.573, derived from general_total 2.293) and `tape_anchor` (−0.01) are allowed to set `predicted_direction = up / mild` even though every sector channel scored 0. The LLM overlay was 0.0, so the signed call came purely from cross-object leakage. Result: predicted up/mild vs actual −0.124% → direction MISS, magnitude MISS.
CORRECTED_BEHAVIOR: When leading_sum = 0 AND divergence = false AND size_gate is applied, the engine must emit **flat/flat** (unsigned), not a signed direction off index_carry. index_carry is a general-market carry term and must not drive a sector call in the absence of any sector-channel signal. Promote the card's "trust the factor card over tape" note to a hard gate: `if leading_sum == 0 and not divergence_flagged: suppress index_carry and tape_anchor from direction determination`.
EVIDENCE: Pipeline JSON: leading_sum 0.0, divergence_flagged False, all S0–S4 = 0.0, overlay_score 0.0, yet predicted_direction 'up', magnitude 'mild', index_carry 0.573, general_total 2.293. Outcome: XLV −0.124% absolute (down), +0.596% relative. Card self-audit: "If the engine tries leftover ES tape_anchor or leftover RS veto, trust this factor card (flat) over tape." Post-session review: "the engine's up/mild was wrong, the card's flat was right."
LESSON_MATCH_CHECK: Matches the 2026-09-22_sector_healthcare_lesson family (engine/tape_anchor overriding a flat factor card on a mixed empty-S1 session) and the 2026-09-22_sector_technology_lesson (v2 publishes down/mild from tape_anchor + index_carry when leading S0–S2 are unsigned). Same failure class: engine signed direction off carry/anchor when the card was flat. This is the healthcare-sector instance of that recurring pattern.
BACKWARD_CHECK: Would this have fired on 09-22 (HC)? Yes — 09-22 had a signed down/mild off a weak S0 + stacked S4 while the card's own logic leaned flat; the gate would have suppressed the signed call. Would it have fired on 09-15/09-16/09-18 (HC up/mild misses)? On those, leading_sum was not necessarily 0, so the gate is scoped correctly to the empty-spine case and would not over-suppress. On 09-17/09-21 (HC up/mild HITs), leading_sum was non-zero (spine present), so the gate correctly does not fire. Backward test passes — the gate is specific to the empty-spine + no-divergence case.
CONFLICT_CHECK: No conflict with active lessons. The 09-22 HC lesson ("prefer flat/mild + shrink confidence when factor sign fights tape") is complementary — today the sign did not fight (both flat) and the card stayed flat, but the engine did not honor it. This lesson extends that experiment to the engine layer. No active lesson mandates emitting a signed direction off index_carry when leading_sum = 0.
FALSIFIER: This lesson is falsified if a future session has leading_sum = 0, divergence = false, size_gate applied, and the sector ETF nonetheless moves ≥±0.5% absolute in a direction predictable from index_carry/tape_anchor alone (i.e., the carry term carried genuine sector information). If such a case recurs with the gate applied and the flat call misses a ≥0.5% move, the gate is too aggressive and should be relaxed to a confidence-shrink rather than a direction suppression.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: The 09-22 HC experiment ("prefer flat/mild + shrink confidence when factor sign fights tape") applies and is working at the card layer — today the card correctly stayed flat. But the engine did not honor it, producing the up/mild miss. The experiment should be extended to suppress engine index_carry when leading_sum = 0. Rolling HC accuracy (dir 0.3 last-10, 0.478 last-30; mag 0.5/0.304) confirms the sector is hard to call directionally on mixed empty-S1 days — reinforcing flat as the correct default. No active lesson conflicts; this is an additive engine-layer gate.
SECTOR: Healthcare
LESSON_END
