# Autopsy — SRoyaltyy/fullscan GitHub Actions

**Window:** 2026-09-05 00:00 ET through 2026-09-08 23:59 ET (EDT = UTC−4).  
**Method:** read-only `gh` API + failed job logs + committed QC packets. No poke, no cancel, no live Pre-Open fire.  
**Branch:** `cursor/preopen-harden-e3a4` (PR #160).  
**Fetched:** 2026-09-08 ~11:55 ET. Two Pre-Open runs were still `in_progress` at fetch.

09-05 Sat / 09-06 Sun = weekend. **09-07 Mon = Labor Day** (NYSE closed). **09-08 Tue = first live session** after Friday 09-04 (the last clean `all_ok=True` packet).

---

## 0. Parent-ready verdict (one screen)

Tuesday 09-08 did not produce a usable pre-open packet. Actions greens lied. The committed stamp at **09:56 ET** is `all_ok=False`, missing `news_parse`, `news_judge`, `general_predict`, `sector_predict`. Two ubuntu Pre-Open writers were **still hung** at autopsy time (`34234748508` since 09:53, `34240616413` since 10:48). HALT `#159` (`a5cfc119`) stopped new writers at 11:12 ET; it could not Force-cancel those two.

The failure is not one bug. It is a stack:

| Class | What happened 09-08 | #160 |
|---|---|---|
| `deepseek_credits` | 402 Insufficient Balance through ~08:12; essays empty | **GAP** |
| `db_timeout` | `canceling statement due to statement timeout` → silent `empty_parse` | **yes** |
| `map_heat_timeout` | refresh 1260s / exit 124, then 09:25 skip | **yes** |
| `skip_0925_gate` | after map-heat burn, skip actions/general/11 sectors | **yes** (late `--bypass-cutoff`) |
| `commit_race` | sleeve-merge / book / weather rebase FATAL | **yes** (safe-push) |
| `concurrency_cancel` | `ubuntu-HHMM` forks could not cancel each other | **yes** (stable group) |
| `cancel_403` / `hung_llm_zombie` | cloud token 403; twins still `in_progress` | **partial** (same-group only; owner Force cancel still required) |
| `pages_perm` / gh-pages lock | Pages id-token timeout; `cannot lock ref gh-pages` | **GAP** |
| ECS git auth | `could not read Username for 'https://github.com'` | **GAP** |
| Heal/Clock | `pipeline_health` schedule **disabled 2026-08-29**; no heal in window | **GAP** |

**Friday 09-04** was the last clean trading packet (`all_ok=True`, 11/11 sectors). Weekend + Labor Day produced books **without** news/predict/heat. Tuesday inherited that hole and then the twin-writer fire.

---

## 1. Inventory — every workflow that actually ran

230 runs in the ET window (plus 148 on Fri 09-04 for pattern). 91 workflows exist in the repo; **15 names fired**.

| Workflow | n | success | fail | cancel | skip | hung | Notes |
|---|---:|---:|---:|---:|---:|---:|---|
| pages build and deployment | 86 | 78 | 1 | 7 | 0 | 0 | GitHub Pages engine |
| Deploy dashboard to GitHub Pages | 51 | 44 | 1 | 0 | 6 | 0 | `workflow_run` child |
| Sleeve merge live card | 19 | 12 | 0 | 0 | 7 | 0 | skipped when book failed / HALT |
| Stock Book ALL (one-shot) | 19 | 12 | 5 | 0 | 2 | 0 | 5× commit FATAL; books still `worst=missing` |
| Boring Winners Backtest | 17 | 8 | 0 | 2 | 7 | 0 | concurrency + HALT |
| **Pre-Open ALL** | 13 | 2 | 5 | 4 | 0 | **2** | one of the two “successes” is quality-failed |
| Daily Orchestrator | 6 | 5 | 0 | 0 | 1 | 0 | 11:22 ET skipped by HALT `if: false` |
| Post-Close ALL | 5 | 5 | 0 | 0 | 0 | 0 | clean Actions; not the 09-08 packet |
| Factor strategy mine | 3 | 3 | 0 | 0 | 0 | 0 | dispatch noise |
| Excel Bot | 2 | 1 | 0 | 1 | 0 | 0 | Sat ok; Tue cancelled ~HALT |
| Finviz ALL | 2 | 2 | 0 | 0 | 0 | 0 | Sat + Tue 01:55 ET |
| Finviz pre-open scrape | 2 | 2 | 0 | 0 | 0 | 0 | Labor Day + Tue |
| Post-Close last-closed | 2 | 2 | 0 | 0 | 0 | 0 | 09-07 sidecar |
| Stock Book readiness | 2 | 2 | 0 | 0 | 0 | 0 | diag only |
| Flatten Lookback Action | 1 | 1 | 0 | 0 | 0 | 0 | Sat dispatch |

**Did not run in window (confirmed):**

| Expected | Evidence |
|---|---|
| Pipeline health / Heal / Clock | Schedule commented out `2026-08-29`. Last runs 2026-08-28, all failure/cancel. **Zero** `*_pipeline_health*` files after 08-28. |
| Map Heat Captain Research (post-close) | Last success **Fri 09-04 03:07 ET** (`33847300349`). None Sat–Tue. |
| `news_parse.yml` / `news_judge.yml` / `news_actions.yml` / `catalyst_daily.yml` / `events_daily.yml` | `n_today=0` in the 09-08 status packet. Folded into Pre-Open ALL; standalone never fired. |
| Collect-*, AB-*, insider, sector_daily, learn_cycle, HIT board, etc. | No runs in the 378-run 09-04..09-08 UTC pull. |

**Clean Actions successes (no quality fail):** ~160 pages/deploy/sleeve/boring/post-close/finviz/factor/flatten/readiness/orchestrator. Summarized by count above. Do **not** treat them as a good 09-08 packet.

---

## 2. Quality-failed “greens” (Actions said success)

### 2.1 Pre-Open ALL `34229210004` — the lie

- **08:59 ET** poke after “zombie cancel”. All three jobs green. Exit 0.
- Log: `[skip_if_good] RUN job=preopen_full date=2026-09-08 packet incomplete`
- `[news_parse] QC FAIL (empty_parse) — throwing out`
- `[preopen-all] skip News actions / General / Per-sector predict (past 09:25 ET)`
- `[preopen-all] DEGRADED 2026-09-08: … missing=['news_parse', 'news_judge', 'general_predict', 'sector_predict'] … Exit 0 so git + Pages still run.`

This is the definition of a quality-failed green. #160 does **not** change that exit-0-on-degraded policy.

### 2.2 HALT `34243224585` — success that wrote nothing

11:12 ET `#159`. Job `STOP all live writers` succeeded; `preopen` + land-book **skipped**. Correct for a hold. Not a packet.

### 2.3 Stock books 09-06 / 09-07 / 09-08 — ranker greens, LLM families missing

Committed `data/stock_book/YYYY_input_health.json`:

| Date | `worst` | missing families | Book file |
|---|---|---|---|
| 09-04 | (packet `all_ok=True`) | none | present, 1.5 MB |
| 09-05 | — | no book (weekend) | absent |
| 09-06 | `missing` | news, heat, general, sector | present (weekend heal) |
| 09-07 | `missing` | news, heat, general, sector | present (Labor Day) |
| 09-08 | `missing` | news, general, sector (heat ok) | present, 1.46 MB @ 10:53 ET |

`learn_grade=true` anyway — the ranker will grade a book that never saw essays.

### 2.4 Committed 09-08 QC packet (the disk of record)

`01_daily/2026-09-08_preopen_status.json` @ **09:56:24 ET**:

- `all_ok=false` `qc_all_ok=false` `book_ok=false`
- missing required: `news_parse`, `news_judge`, `general_predict`, `sector_predict`
- attempts: **weather only**
- `01_daily/news/2026-09-08_{parsed,judge.md,actions}.json` **absent**
- `01_daily/general/2026-09-08_predict.md` **absent**
- `01_daily/sectors/2026-09-08/*_predict.md` **0 files**
- Finviz digest on disk is actually full (`digest_n=400`, 86 KB). The 09:56 QC row that showed `size=2` was a race/empty snapshot; later scrape landed.
- `map_heat.json` on disk is 271 KB (not the 27-byte stub in the early QC row).
- `2026-09-08_research.md` exists (34 KB) — night/passthrough baseline, not a morning refresh win.

### 2.5 Dashboard stamp stuck

`dashboard/index.html` still embeds **`2026-09-04T08:09:42-04:00`**. Sleeve-merge HTML has 09-08 strings. Chrome Pages can look “live” while the main card is Friday.

---

## 3. Day-by-day

### Fri 09-04 (pattern only) — last clean packet

- Pre-Open `33855441892` dispatch **success**. Disk: `all_ok=True`, 11 sectors, parsed/judge/actions present.
- 148 Actions runs: 133 success, 13 cancel (pages + boring churn), 2 fail:
  - Deploy `33879562921`: `cannot lock ref 'refs/heads/gh-pages'`
  - Sleeve card `33880765702`: `CONFLICT … dashboard/sleeve-merge/index.html` → `[safe-push] FATAL`
- Post-Close `33885419832` cancelled (hung-sector kill). **Same commit_race + pages lock already visible Friday.**

### Sat 09-05 — 31/31 success

No pre-open (weekend gate). Finviz ALL + digest landed. Excel Bot success. Post-Close / Flatten / Factor dispatch. No packet, no book — expected.

### Sun 09-06 — 8 success + 1 fail

- **pages `34029321216` FAIL** — `Failed to get ID Token` / `Ensure GITHUB_TOKEN has permission "id-token: write"`.
- Stock Book dispatch `34051999973` **success** → weekend book with `worst=missing` (quality-failed green).

### Mon 09-07 Labor Day — 2 fail + 2 skip

- **Pre-Open `34099583763` FAIL** (owner dispatch 04:14 ET):
  1. Finviz overlay: `post-close map heat missing: …/2026-09-07_map_heat.json — industry groups must be scraped at 22:00 ET` → exit 1.
  2. ECS `preopen` then: `fatal: could not read Username for 'https://github.com'` × retries → `[safe-push] FATAL`. Artifact upload `403 Forbidden: job is completed`.
- **Stock Book `34150949716` FAIL** 14:16 ET: `CONFLICT` flatten_card + sleeve `positions.json`/`today.json` → FATAL. Child Boring/Sleeve **skipped**.
- Books/weather exist; **no** 09-07 parse/judge/predict/map_heat. Overlay had nothing to stand on Tuesday morning.

### Tue 09-08 — the fire

| ET | Run | What |
|---|---|---|
| 04:17–04:49 | Post-Close / Factor / **ECS Pre-Open `34206618217`** + Book `34206618950` | ECS commit: Username/auth FATAL after writing research. Book: sleeve-merge CONFLICT FATAL. |
| 06:21 | ECS poke `34215042304` | Queued **~88 min** (created 10:22Z, started 11:50Z). Step 9 left `in_progress`; job marked failure. `runner_lost` / zombie. |
| 06:57 | Ubuntu `34218127528` | **DeepSeek 402** on events/predict/11 sectors. `empty_parse`. Land-book weather/book CONFLICT FATAL. |
| 07:27 | Deploy `34220811284` | gh-pages `cannot lock ref`. |
| 08:12 | Ubuntu heal `34224791062` | Still **402**. DB auth 15s + statement timeout. Run **cancelled**; commit still FATAL. |
| 08:49 | Top-up poke `34228319968` | **Cancelled, no jobs** (superseded before start). |
| 08:59 | `34229210004` | **Actions success / QC DEGRADED** (see §2.1). DeepSeek alive (caps, no 402). |
| 09:00 | `34229320047` `ubuntu` new group | DB timeout → empty_parse. map-heat **1260s / exit 124**. Skip 09:25. sleeve-merge CONFLICT FATAL. |
| 09:14–09:56 | Book `34230706335` `34233669901` `34235110621` | Same commit_race vs sleeve-merge / flatten_card / stock_book files. |
| 09:47 / 09:49 | `34234132322` `34234401160` | Cancelled (forked groups). 09:45 jobs actually finished green then run cancelled. |
| **09:53** | **`34234748508`** force-bypass | **HUNG** — `preopen` step 9 `in_progress` at autopsy (~6h). Scrape + land-book already green. |
| **10:48** | **`34240616413`** “supersede hung force writer” | **HUNG** — same. Step “Cancel hung 34234748508” concluded **success** but the zombie **did not die**. |
| 10:43 | Excel Bot `34240092081` | Cancelled 11:13 ET (HALT cancel-all). |
| 11:12 | HALT `a5cfc119` | New writers skipped. Zombies not cancelled. |

---

## 4. War-room paste

Format: `workflow · run_id · URL · conclusion · failing job/step · root-cause class · evidence quote · #160 covers?`

### 4.1 Failures

```
pages-build-deployment · 34029321216 · https://github.com/SRoyaltyy/fullscan/actions/runs/34029321216 · failure · deploy / Deploy to GitHub Pages · pages_perm · "Failed to get ID Token" / "Ensure GITHUB_TOKEN has permission \"id-token: write\"." · gap

Pre-Open ALL · 34099583763 · https://github.com/SRoyaltyy/fullscan/actions/runs/34099583763 · failure · Finviz scrape / Digest + morning overlay · other (holiday overlay hard-fail) · "post-close map heat missing: .../2026-09-07_map_heat.json — industry groups must be scraped at 22:00 ET" · gap
Pre-Open ALL · 34099583763 · (same run, second job) · failure · preopen / Commit predictive artifacts · other (ECS git auth) · "fatal: could not read Username for 'https://github.com': terminal prompts disabled" · gap

Stock Book ALL · 34150949716 · https://github.com/SRoyaltyy/fullscan/actions/runs/34150949716 · failure · all / Commit everything · commit_race · "CONFLICT (content): Merge conflict in data/sleeve_merge/positions.json" + "[safe-push] FATAL: push failed after retries" · yes (safe-push leftover resolver; book workflow itself not re-grouped)

Stock Book ALL · 34206618950 · https://github.com/SRoyaltyy/fullscan/actions/runs/34206618950 · failure · all / Commit everything · commit_race · "CONFLICT … dashboard/sleeve-merge/index.html" + "data/sleeve_merge/today.json" + FATAL · yes

Pre-Open ALL · 34206618217 · https://github.com/SRoyaltyy/fullscan/actions/runs/34206618217 · failure · preopen / Commit predictive artifacts · other (ECS git auth) · "fatal: could not read Username for 'https://github.com'" ; artifact "403 Forbidden: job is completed" · gap

Pre-Open ALL · 34215042304 · https://github.com/SRoyaltyy/fullscan/actions/runs/34215042304 · failure · preopen / Pre-Open ALL (predictive + QC) · runner_lost · ECS queued ~88m then step 9 left in_progress; job conclusion=failure; failed-log empty · partial (ubuntu poke exists; ECS cancel-in-progress stays false)

Pre-Open ALL · 34218127528 · https://github.com/SRoyaltyy/fullscan/actions/runs/34218127528 · failure · Land book / Commit book layer · commit_race · "CONFLICT … 01_daily/weather/2026-09-08_weather.json" + FATAL · yes
Pre-Open ALL · 34218127528 · (same run) · failure · preopen / Commit predictive artifacts · deepseek_credits + db_timeout · "DeepSeek 402 Payment Required: {\"error\":{\"message\":\"Insufficient Balance\"}}" ; "[news_parse] QC FAIL (empty_parse)" · credits=gap ; db=yes

Deploy dashboard · 34220811284 · https://github.com/SRoyaltyy/fullscan/actions/runs/34220811284 · failure · deploy / Publish to GitHub Pages · pages_perm · "cannot lock ref 'refs/heads/gh-pages': is at f61682a… but expected 54dcface…" · gap

Pre-Open ALL · 34229320047 · https://github.com/SRoyaltyy/fullscan/actions/runs/34229320047 · failure · preopen / Commit predictive artifacts · db_timeout + map_heat_timeout + skip_0925_gate + commit_race · "canceling statement due to statement timeout" ; "timed out after 1260s: … map_heat_refresh" ; "skip General market predict (past 09:25 ET)" ; "CONFLICT … dashboard/sleeve-merge/index.html" ; "Map heat morning delta refresh: exit 124" · yes (all four)

Stock Book ALL · 34230706335 · https://github.com/SRoyaltyy/fullscan/actions/runs/34230706335 · failure · all / Commit everything · commit_race + db_timeout · "canceling statement due to statement timeout" ; "CONFLICT … 01_daily/2026-09-08_flatten_card.md" ; FATAL · yes

Stock Book ALL · 34233669901 · https://github.com/SRoyaltyy/fullscan/actions/runs/34233669901 · failure · all / Commit everything · commit_race · "past 09:25 ET — skip LLM + extras" then sleeve/book CONFLICT FATAL · yes / skip is book-by-design

Stock Book ALL · 34235110621 · https://github.com/SRoyaltyy/fullscan/actions/runs/34235110621 · failure · all / Commit everything · commit_race · "CONFLICT … 01_daily/2026-09-08_stock_book.md" + FATAL · yes
```

### 4.2 Hung (still in_progress at fetch)

```
Pre-Open ALL · 34234748508 · https://github.com/SRoyaltyy/fullscan/actions/runs/34234748508 · in_progress · preopen / Pre-Open ALL (predictive + QC) · hung_llm_zombie + cancel_403 · title "poke Pre-Open ALL 09:50 ET — force bypass 09:25 gate" ; step 9 started 13:56:07Z, completed=null at ~15:55Z ; later run's "Cancel hung 34234748508" job concluded success but this run did not die · partial (needs owner Force cancel; forked ubuntu-HHMM group)

Pre-Open ALL · 34240616413 · https://github.com/SRoyaltyy/fullscan/actions/runs/34240616413 · in_progress · preopen / Pre-Open ALL (predictive + QC) · hung_llm_zombie + cancel_403 · title "supersede hung force writer" ; step 9 started 14:50:55Z, completed=null ; live job logs 404 · partial
```

Job logs for both hung jobs returned HTTP 404 (in-progress logs not downloadable). Evidence is job payload + titles, not step stdout.

### 4.3 Cancelled

```
Pre-Open ALL · 34224791062 · https://github.com/SRoyaltyy/fullscan/actions/runs/34224791062 · cancelled · preopen / Commit predictive artifacts · deepseek_credits + db_timeout + concurrency_cancel · "DeepSeek 402 … Insufficient Balance" ; "Failed to connect to database: authentication did not complete within 15000ms" ; run cancelled after ~1h · credits=gap ; db=yes ; cancel=yes

Pre-Open ALL · 34228319968 · https://github.com/SRoyaltyy/fullscan/actions/runs/34228319968 · cancelled · (no jobs) · concurrency_cancel · title "after DeepSeek top-up" ; cancelled before a runner · yes

Pre-Open ALL · 34234132322 · https://github.com/SRoyaltyy/fullscan/actions/runs/34234132322 · cancelled · (jobs all success, then run cancelled) · concurrency_cancel · title "09:45 ET — re-fire after commit-fail" ; superseded by later ubuntu-HHMM fork · yes

Pre-Open ALL · 34234401160 · https://github.com/SRoyaltyy/fullscan/actions/runs/34234401160 · cancelled · (no jobs) · concurrency_cancel · title "FORCE Pre-Open ALL on push — bypass 09:25 skip" ; cancelled before start · yes (and #160 removes permanent --force)

pages-build-deployment · 34230696137 · https://github.com/SRoyaltyy/fullscan/actions/runs/34230696137 · cancelled · deploy · concurrency_cancel · Pages engine superseded · n/a (GitHub-owned)

pages-build-deployment · 34230731461 · https://github.com/SRoyaltyy/fullscan/actions/runs/34230731461 · cancelled · build · concurrency_cancel · same · n/a

pages-build-deployment · 34233303630 · https://github.com/SRoyaltyy/fullscan/actions/runs/34233303630 · cancelled · build · concurrency_cancel · same · n/a

pages-build-deployment · 34233730551 · https://github.com/SRoyaltyy/fullscan/actions/runs/34233730551 · cancelled · build · concurrency_cancel · same · n/a

pages-build-deployment · 34233754518 · https://github.com/SRoyaltyy/fullscan/actions/runs/34233754518 · cancelled · build · concurrency_cancel · same · n/a

pages-build-deployment · 34235443156 · https://github.com/SRoyaltyy/fullscan/actions/runs/34235443156 · cancelled · build · concurrency_cancel · same · n/a

pages-build-deployment · 34239367835 · https://github.com/SRoyaltyy/fullscan/actions/runs/34239367835 · cancelled · deploy / Deploy to GitHub Pages · concurrency_cancel · deploy step cancelled mid-push · n/a

Boring Winners · 34233782460 · https://github.com/SRoyaltyy/fullscan/actions/runs/34233782460 · cancelled · seat / Seat · concurrency_cancel · superseded by later book child · gap (not in #160)

Boring Winners · 34235342323 · https://github.com/SRoyaltyy/fullscan/actions/runs/34235342323 · cancelled · seat / Seat · concurrency_cancel · same · gap

Excel Bot · 34240092081 · https://github.com/SRoyaltyy/fullscan/actions/runs/34240092081 · cancelled · run / Run fetch + engine + signals + tracking · other (HALT cancel-all) · started 10:43 ET, cancelled 11:13 ET as #159 "Cancel every in-progress / queued run" · gap
```

### 4.4 Quality-failed successes (must treat as degraded)

```
Pre-Open ALL · 34229210004 · https://github.com/SRoyaltyy/fullscan/actions/runs/34229210004 · success · preopen / Pre-Open ALL (predictive + QC) · db_timeout + skip_0925_gate · "QC FAIL (empty_parse)" ; "DEGRADED 2026-09-08: … Exit 0 so git + Pages still run." ; missing parse/judge/general/11 sectors · db/skip=yes ; exit-0-on-degraded=gap

Pre-Open ALL · 34243224585 · https://github.com/SRoyaltyy/fullscan/actions/runs/34243224585 · success · STOP all live writers · other (HALT) · preopen+book skipped ; no 09-08 salvage · intended (#159); #160 swallows further 09-08 pokes

Stock Book ALL · 34051999973 · https://github.com/SRoyaltyy/fullscan/actions/runs/34051999973 · success · all · other (weekend book, no packet) · 09-06 input_health worst=missing news/heat/general/sector · gap (holiday/weekend packet policy)

Stock Book ALL · 34106153976 / 34140231651 / 34144460077 · (Labor Day greens) · success · all · other (holiday book, no packet) · 09-07 input_health worst=missing ; no parse/predict/map_heat · gap

Stock Book ALL · 34209855747 / 34218127404 / 34220811291 / 34226040649 / 34229217526 / 34233243305 / 34234779221 / 34238722247 · success · all · skip_0925_gate · several logs: "past 09:25 ET — skip LLM + extras so the book can still land" ; landed book worst=missing news/general/sector · partial (book skip-LLM after 09:25 is unchanged on purpose)
```

### 4.5 Skipped (not war-roomed one-by-one)

23 skips: HALT `if: false` (book/deploy/boring/sleeve/orchestrator @ 11:12–11:22) + `workflow_run` children of failed/skipped parents. Expected. Not root causes.

---

## 5. #160 coverage map

PR: https://github.com/SRoyaltyy/fullscan/pull/160  
Head: `c2e00f03` on `cursor/preopen-harden-e3a4`. Based on HALT `a5cfc119`.

| #160 claim | Autopsy verdict | Notes |
|---|---|---|
| 1 Twin writers / cancel 403 | **Confirmed.** `ubuntu-0945` / `0950` / `stop-1108`. `34240616413` “Cancel hung 34234748508” = success, zombie still `in_progress`. | Fix: stable `preopen-all-ubuntu` + `cancel-in-progress: true` + `actions: write`. **Does not kill already-forked groups.** |
| 2 Commit race sleeve-merge / packet | **Confirmed** on `34229320047`, `34218127528`, all 5 book FATALS, Fri sleeve `33880765702`. | Fix: take origin/main for `dashboard/`; keep OUR packet/ranker; leftover unmerged resolver. |
| 3 news_parse DB timeout → empty | **Confirmed** on `34218127528`, `34224791062`, `34229320047`, `34229210004`, `34230706335`. | Fix: 90s `statement_timeout`, bounded query, 3× retry, QC token `db_timeout` not silent `empty_parse`. |
| 4 Past-09:25 skip + empty `inputs.force` | **Confirmed.** 08:59 + 09:00 greens/fails skipped essays after map-heat burn. Permanent `--force` on later pokes created the 09:50 zombie. | Fix: on-time skip stays; push 09:25–12:00 `--bypass-cutoff`; only dispatch `force=true` rewrites. |
| 5 map_heat 1260s / 124 | **Confirmed** on `34229320047`. | Fix: ~480s HTTP / ~540s subprocess; 124 → `--passthrough` night baseline. |

### Manager GAP list (not fixed by #160)

1. **`deepseek_credits`** — 402 Insufficient Balance wiped events + predict + 11 sectors on `34218127528` and `34224791062`. No preflight balance check, no loud QC class, no “do not start essays” gate. Out of #160 scope; **still a 09-09 killer if the key is empty**.
2. **`hung_llm_zombie` still live** — `34234748508` and `34240616413` remain `in_progress`. #160 only cancels the **same** ubuntu group. Owner must Force-cancel these two before 05:55 ET 09-09 or they can steal the box / race commits. Cloud `gh run cancel` = 403 (documented, not fixed).
3. **ECS git auth** — `could not read Username for 'https://github.com'` on `ecs-openclaw` (`34099583763`, `34206618217`). Ubuntu commits work. Morning ECS 05:55 **will fail the packet commit** if this persist-credentials/token hole is still there.
4. **`pages_perm` / gh-pages lock** — Sat id-token timeout; Fri+Tue `cannot lock ref gh-pages`. #160 makes HTML push best-effort and drops `dashboard/` from the packet commit — reduces blast radius, **does not fix Pages**.
5. **Holiday / missing night baseline** — Labor Day Pre-Open hard-failed overlay because `2026-09-07_map_heat.json` was never scraped (map_heat_postclose last ran Fri). Tuesday morning then started from a hole. No holiday gate; no “use last session baseline” for overlay.
6. **`pipeline_health` / Heal clock off since 2026-08-29** — zero heals in the window. The 07:00 ET preopen health pass that would have screamed `empty_parse` / missing predict never ran.
7. **Actions green on DEGRADED** — `Exit 0 so git + Pages still run` (`34229210004`). War-room will keep seeing greens that are empty. #160 does not fail the job when `all_ok=false`.
8. **Stock Book ALL concurrency** — #160 only **reverts HALT** on `stock_book_all.yml`. Book vs sleeve-merge vs Pre-Open land-book still collide. safe-push helps the commit; it does not stop the twin writers.
9. **Excel Bot / Boring Winners** — cancelled by HALT or book-child concurrency. Not in #160.
10. **`dashboard/index.html` stamp stuck on 09-04** — Chrome can show Friday while 09-08 book JSON exists. #160 explicitly does **not** own theme-radar / dashboard stamp repair.
11. **ECS queue `runner_lost`** — `34215042304` sat ~88 minutes then died with step 9 still `in_progress`. #160 prefers ubuntu on **push**; systemd 05:55 is still ECS. If the runner is busy/offline, the clock job can zombie again.
12. **Standalone news/catalyst never ran** — not a bug if Pre-Open owns them; it **is** a gap if Pre-Open skips them after 09:25 and nothing else writes `*_actions.json`.

### What #160 does **not** claim (correctly out of scope)

Live 09-08 salvage, trading logic, theme-radar, DeepSeek billing, Pages IAM, ECS runner token, pipeline_health re-enable.

---

## 6. 09-09 implications (no poke)

- Merge #160 **tonight** is OK (09-08 push swallow). Do **not** re-touch `preopen_all.yml` after midnight ET.
- **Owner Force-cancel** `34234748508` and `34240616413` if they are still running at merge time.
- Confirm DeepSeek balance **before** 05:55 ET. A 402 repeats the empty-essay day even with every #160 fix.
- Confirm ECS `git push` credentials (the Username hole). If unsure, first clock should be watched; ubuntu heal via **same** `preopen-all-ubuntu` group only.
- Read `01_daily/2026-09-09_preopen_status.md` for `db_timeout` vs `empty_parse`, and map-heat passthrough (OK).
- Do not invent `ubuntu-HHMM`. Do not pass `--force` on the morning ECS start.

---

## 7. Source index

- Runs: `GET /repos/SRoyaltyy/fullscan/actions/runs?created=2026-09-04T00:00:00Z..2026-09-09T08:00:00Z` → 378 rows; 230 in ET window.
- Jobs: `/actions/runs/{id}/jobs` for 82 keyed runs.
- Failed logs: `gh run view {id} --log-failed` (hung/in-progress = empty or 404).
- Disk: `01_daily/2026-09-0{4,8}_preopen_{status,qc}.json`, `data/stock_book/2026-09-0{6,7,8}_input_health.json`.
- #160 files: `preopen_all.yml`, `safe_git_push.sh`, `src/db.py`, `src/news_parse.py`, `src/output_qc.py`, `src/map_heat_refresh.py`, `src/run_preopen_all.py`, `00_grounding/preopen_unattended.md`.
