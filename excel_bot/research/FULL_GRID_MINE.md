# Full workbook (A–JO) → H/I mine

_Generated 2026-09-07. Research only. Live `flatten_robust` frozen. Whole sheet, not A–O._

## Status

Full A–JO dump is in progress (`research/all_cols_grids/`, gitignored).
The AAPL-only smoke (1 name, 139 days) is **not** a result — it cannot
clear ticker-holdout or the ship bar.

This file will be overwritten by `engine/mine_full_grid.py` once the
universe dump is on disk. Every letter the engine paints is gated
(values, quintiles, fills, lags, text) and AND-ed with the morning
light / green O cluster.

Research only. No cards. No live wire.
