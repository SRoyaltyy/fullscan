# Sector Reflect — Consumer Defensive — 2026-09-03

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: Sector ETF prediction where the prior session had a >+1% relative FTS day (XLP 3d rel +1.30% paid on 09-01), the current morning has flat futures (ES +0.13% / NQ +0.16%), and the model treats the follow-through as already-paid and scores S4=0 with flat/flat — but the actual session delivers XLP −0.32% vs SPY +1.05% (rel −1.36%) because the model applied the follow-through rule asymmetrically: it used the prior FTS day to cap magnitude at flat but did not consider that a flat-futures morning after an outsized relative day can reverse hard when the broad market rips on a macro catalyst (ISM Services beat) that the model explicitly identified as two-sided and refused to pre-score.
CURRENT_BEHAVIOR: When a sector had a >+1% relative FTS day 1–2 sessions prior and the current morning shows flat futures with no fresh sector catalyst, the model scores S4=0 (follow-through already paid), S0=0 (two-sided macro not pre-scored), and predicts flat/flat — treating the prior FTS as confirmation of a completed move rather than as a potential exhaustion event that sets up mean-reversion when the macro tape flips risk-on.
CORRECTED_BEHAVIOR: When a sector had a >+1% relative FTS day 1–2 sessions prior and the current morning shows flat futures with a same-day two-sided macro catalyst (ISM Services, claims) that could flip the tape risk-on, the model must treat the prior FTS as a two-sided object: it caps upside magnitude (no second FTS up-day) AND it licenses downside risk if the macro catalyst resolves risk-on (which unwinds the defensive bid). Specifically: (a) if the two-sided catalyst is high-impact and the sector's 1d relative is already negative (XLP 1d rel −0.12%), score S4 as −0.5 to −1 rather than 0, because the FTS bid is the marginal positioning that gets sold when SPY rips; (b) do not let "two-sided" mean "zero" when the sector's recent relative outperformance creates asymmetric downside to a risk-on resolution; (c) the follow-through rule must be symmetric — it caps magnitude in both directions (no second FTS up, but also no protection from FTS unwind).
EVIDENCE: 2026-09-03 XLP: predicted up/flat (total 1.35, S4=0), actual −0.32% vs SPY +1.05% (rel −1.36%). The model explicitly identified ISM Services as two-sided (08-12 analog) and refused to pre-score it, then the ISM beat flipped the tape risk-on and XLP's residual defensive bid (built on 09-01's +1.30% rel FTS) was sold. The 1d rel was already −0.12% on 09-02 — the FTS unwind had started before the ISM print. This mirrors the Basic Materials lesson from the same date (XLB −0.62% vs SPY +1.05%, rel −1.67%) where a prior >+1% relative day was followed by a hard reversal on the same macro catalyst.
LESSON_MATCH_CHECK: Matches 2026-09-03_sector_basic_materials_lesson.md (same date, same macro catalyst, same pattern: prior >+1% relative day + flat futures + two-sided macro = hard reversal). Also matches 2026-09-03_sector_communication_services_lesson.md (same date, same macro catalyst, but opposite direction — XLC gained because it had no FTS bid to unwind). The Consumer Defensive case is the FTS-unwind variant: the sector with the residual defensive bid gets sold hardest when the macro flips risk-on.
BACKWARD_CHECK: 2026-08-28: predicted down/mild, XLP +0.43% vs SPY −0.23% (rel +0.66%) — dir MISS. That was an anti-FTS day that did NOT fire (no NQ lead, no vol spike), and XLP held up. But the setup differed: no prior >+1% FTS day in the preceding 2 sessions (08-27 was down −1.38%). The 08-28 case had no residual FTS bid to unwind. 2026-08-27: predicted down/mild, XLP −1.38% — dir HIT. That was a genuine risk-off day. The 09-03 case is distinct: prior FTS bid + risk-on resolution = unwind. Backward check passes — the corrected behavior would not have changed 08-28 (no FTS bid present) or 08-27 (risk-off, not risk-on).
CONFLICT_CHECK: Conflicts with the 08-28 DO-INSTEAD lesson ("prefer flat/mild when sign fights tape") — but that lesson applies when the model is trying to force a directional call against the tape. Here the tape was genuinely two-sided pre-ISM, and the correction is not about forcing direction but about recognizing that a residual FTS bid creates asymmetric downside to a risk-on resolution. No conflict with the "keep direction, shrink confidence on modest |score|" lesson — that lesson governs confidence, not the sign of S4. The correction is consistent with the 08-21 level-vs-change lesson (a 1–2 bp dip is stabilization, not duration relief) — the model should have recognized that 09-01's FTS was a level event that could unwind.
FALSIFIER: A future session where a sector has a prior >+1% relative FTS day, flat futures, a two-sided macro catalyst that resolves risk-on, and the sector does NOT underperform (rel > −0.5%) would falsify this lesson. Also falsified if the sector's 1d relative was positive (not already fading) going into the macro print, suggesting the FTS bid was still building rather than unwinding.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: The 08-28 DO-INSTEAD lesson (prefer flat/mild when sign fights tape) was applied correctly — the model did not force a down call. The failure was in S4=0: the model treated the prior FTS as fully paid and scored zero, when the residual bid created unwind risk. The "keep direction, shrink confidence" lesson was applied (confidence 0.52) but did not address the sign error in S4. The 08-14 lesson (do not copy 3d FTS into S1+S2+S4) was applied correctly — the model did not restack the FTS as an up signal. The missing lesson is the symmetric follow-through rule: prior FTS caps upside AND licenses downside on risk-on resolution.
SECTOR: Consumer Defensive
LESSON_END
