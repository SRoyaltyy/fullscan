# Sector Reflect — Utilities — 2026-09-11

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open and the sector is a low-beta defensive bond-proxy (XLU/XLRE/XLP); the pre-binary tape is risk-on-leaning (green ES/NQ futures, oil offering hard, VIX <20 and backwardated); the model identifies the binary as "two-sided / unresolved" and therefore treats it as symmetric, scoring S0=0 and capping confidence/multiplier — when in fact BOTH branches of the binary are negative-to-neutral for a defensive bond-proxy (in-line → risk-on rotation away from defensives; hot → rates up, bond proxy down), so the binary is asymmetric-negative and the correct call is down/mild with a relative-lag clause.
CURRENT_BEHAVIOR: Model writes "do not pre-score either branch" (per 09-03/09-04), scores S0=0, treats green futures + oil-offering as *offsets* to the sticky-high long end, caps confidence at 0.55 and multiplier at 0.9, and emits down/flat. The 09-10 gate (VIX <20 backwardated → rising long end = relative-lag signal) fires in prose but is not acted on in the scores.
CORRECTED_BEHAVIOR: When the dominant binary is a scheduled macro release and the sector is a defensive bond-proxy, test the sign of EACH branch against the sector's factor exposure before calling it symmetric. If both branches are negative-to-neutral (in-line CPI → risk-on rotation away from defensives; hot CPI → rates up → bond-proxy down), score the asymmetry: S0 = −1 (not 0), raise S1 rotation-away from PARTIAL to full weight, weight the LIVE 1d/1m relative tape over stale 3d/1w positives in S2, raise S4 to −1, raise confidence to ~0.70 and multiplier to ~1.0, and emit down/mild with an explicit relative-lag clause. Also: test the sign of every "offset" — for a defensive, risk-on inputs (green futures, oil-offering) are headwinds, not cushions. And when the 09-10 gate fires, ACT on it (raise conviction), don't cap it.
EVIDENCE: 2026-09-11 XLU −0.31% absolute / SPY +0.85% / rel −1.16%. CPI in-line (+0.4% MoM / +3.4% YoY headline; +0.3% / +2.4% core). Morning scored S0=0, S1=−1, S2=0, S3=0, S4=−0.5, mult 0.9, conf 0.55 → down/flat. Direction HIT, magnitude HIT by band letter (|−0.31%| < 0.5%), but the −1.16% relative lag was unflagged and every input needed to call down/mild with a relative-lag clause was on the desk at the open (green futures, oil −2.78%/−3.37%, 10Y 4.83%/30Y 5.28% sticky-high, VIX 17.24 backwardated, 1d rel −0.38%, 1m rel −0.89%). Miss was inferential, not informational.
LESSON_MATCH_CHECK: Strong match to 2026-09-11_sector_consumer_defensive_lesson.md ("pre-binary tape already risk-on-leaning (green ES/NQ/Europe futures, oil offering, VIX term structure in backwardation explicitly flagged as a CPI-day hedge); AND the sector is a low-beta defensive bond-proxy (XLP/XLU/XLRE)... model identifies the benign branch..."). Also matches 2026-09-11_sector_healthcare_lesson.md (sign of S0 inverted for the defensive sector on a falling-oil + green-futures + matched-CPI tape) and 2026-09-11_sector_real_estate_lesson.md (stale multi-horizon relative lag scored into both S2 and S4). This is the same cross-sector failure mode firing in Utilities — the candidate lessons from the sibling sectors on the same date are the same trigger.
BACKWARD_CHECK: Would this lesson have changed the 09-11 Utilities call? Yes. Applying it: S0 0→−1, S1 −1→−1.5/−2, S2 0→−0.5, S4 −0.5→−1, conf 0.55→~0.70, mult 0.9→~1.0 → down/mild with relative-lag clause. Direction unchanged (already HIT), but magnitude band moves flat→mild (still a HIT by the letter either way since |−0.31%| < 0.5%, but the reasoning and the relative-lag clause would have been correct). The headline miss — the unflagged −1.16% relative lag — would have been addressed. Backward test passes: the lesson is actionable and would have improved the call's conviction and relative framing.
CONFLICT_CHECK: No conflict with active lessons. 09-03/09-04 ("do not call flat into a high-impact binary; widen the band") is REFINED, not contradicted — the refinement is that for a bond proxy the binary is one-sided, so the correct non-flat call is down/mild, not flat. 09-10 gate ("VIX <20 backwardated → rising long end = relative-lag signal") is REINFORCED — this lesson says act on it. 08-18 ("risk-off + rising long end → relative beat") does not conflict — today was not risk-off. 08-25 ("S0=S1=0 → do not manufacture down") does not conflict — S1 was not 0. 08-12 ("AI-power is a 1d dampener, not a band engine") is consistent — the AI-power cohort cushioned the absolute drawdown but could not offset the regulated-cohort drag. No conflict.
FALSIFIER: The lesson is falsified if, on a future session with (a) a pending high-impact macro binary, (b) a low-beta defensive bond-proxy sector, (c) risk-on-leaning pre-binary tape (green futures, oil offering, VIX <20 backwardated), and (d) sticky-high long end, the sector ETF closes UP or flat-to-positive on a relative basis (rel ≥ 0) — i.e., the defensive does NOT lag. Specifically: if XLU/XLRE/XLP closes with rel ≥ 0 on such a setup, the "asymmetric-negative binary for bond proxies" claim is wrong and the symmetric treatment was correct.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: The 09-10 gate was correctly applied in prose but not acted on in the scores — this is the core actionable gap. The 09-03/09-04 lesson (do not call flat into a high-impact binary) was applied to forbid flat but the model then rounded down to flat anyway, which is internally inconsistent. The 08-12 lesson (AI-power = dampener, not engine) held and is confirmed by the modest absolute decline vs. severe relative lag. The 08-28 lesson (do not promote a single-name regulatory item) held. The 08-25 lesson (do not manufacture down from carried lag) did not bind because S1 was not 0. The candidate lessons from the sibling sectors on 2026-09-11 (consumer_defensive, healthcare, real_estate) are the same trigger and should be consolidated into a single cross-sector lesson: "pending macro binary + defensive bond-proxy + risk-on pre-binary tape = asymmetric-negative binary; score down/mild with relative-lag clause; do not treat the binary as symmetric."
SECTOR: Utilities
LESSON_END
