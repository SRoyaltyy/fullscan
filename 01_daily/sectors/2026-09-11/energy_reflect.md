# Sector Reflect — Energy — 2026-09-11

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A scheduled high-impact macro binary (CPI/NFP/FOMC) is pending at the open; US index futures are independently confirming a directional move of ≥+0.5% (ES/NQ/Russell all green); the sector ETF is a commodity/beta sector whose own underlying (crude) is offered but NOT collapsing (a ~2–3% dip, not a >5% break); AND the sector carries a crowded-long / record-close sequence. The model identifies the risk-on tape but assigns it the WRONG SIGN for the sector's absolute print — scoring S0 as a headwind ("risk-on is a mild headwind for a commodity sector when the barrel is offered") instead of a tailwind — while simultaneously betting S1 on the offered barrel's PERSISTENCE. The two components offset rather than align, and the absolute direction call resolves opposite to the realized close.
CURRENT_BEHAVIOR: On 2026-09-11 the model scored S0 = 0 with prose explicitly framing green futures as "a mild headwind for a commodity sector when the barrel is offered," and S1 = −1 on the morning oil-offered print (WTI −2.54%, CL=F −2.78%), netting to down/mild. XLE closed +0.32% absolute (direction MISS) but −0.53% relative (direction HIT on relative). The barrel did not extend: WTI finished ~$100.36, ABOVE the $99.91 morning snapshot. The model's own divergence flag ("leading-vs-tape divergence — factors lean down, tape is flat/risk-on") correctly warned of the tension but the sign error on S0 was never corrected.
CORRECTED_BEHAVIOR: When a scheduled macro binary is pending and index futures are independently confirming a directional move of ≥+0.5% across all four indices, score equity beta as a TAILWIND for the sector's ABSOLUTE print (S0 ≥ 0, and positive for high-beta/commodity sectors on risk-on days), and let the sector's own underlying (the barrel) set only the RELATIVE sign. A non-confirming or mildly-offered underlying caps participation; it does not force an absolute down-close when the broad tape is bid. Concretely: (a) do not assign S0 a negative sign on a green-futures CPI day for a beta sector; (b) when S1 rests on the PERSISTENCE of a live intraday print (an offered barrel), require the print to be a break (>5%) rather than a dip (~2–3%) before letting it set the absolute direction; (c) if the model's own divergence flag fires because "factors lean down, tape is flat/risk-on," treat that as a signal to FLATTEN the absolute call (flat/mild) rather than to keep the down sign at reduced conviction.
EVIDENCE: Outcome: XLE +0.32% (open 64.89 → close 65.14), SPY +0.85%, rel −0.53%. Morning snapshot: ES +0.63%, NQ +0.65%, Russell +0.63%, DJIA +0.53%. Morning oil: WTI $99.91 / Brent $104.62; close oil: WTI ~$100.36 / Brent ~$104.42 (Convex, 2026-09-11) — the offered print did not extend and WTI finished higher. Morning HIT_GRID marked "Risk-on tape / equity beta expansion" as MISS at 0.50 — that grid call was wrong; risk-on was the session's dominant driver. Scoreboard: direction_hit False, magnitude_hit True. Rolling last-10 dir=0.3 mag=0.3.
LESSON_MATCH_CHECK: Partial match to 2026-09-11_sector_consumer_cyclical_lesson.md ("scheduled high-impact macro binary pending; futures confirming ≥+0.5%; sector's own live inputs aligned with futures direction; model scores the conditional asymmetry as base case, emitting negative S0/S1 despite every live non-stale input pointing the other way") — same root failure mode (mis-scoring the pre-binary risk-on tape against a sector whose live inputs are being read as bearish). Also matches 2026-09-11_lesson.md (CPI pending + futures confirming ≥+0.5% + narrative lean agrees with futures but pipeline maps to flat). Does NOT match 2026-09-10_sector_energy_lesson.md (that lesson's trigger was a record-close sequence + 1m rel ≥ +8% with a SUB-1.5% marginal factor increment; here the increment was −2.5% to −3.4%, a different regime). The 09-10 Energy lesson FIRED as designed and its discipline (cap magnitude, cut conviction, don't double-count) was correct — the failure is orthogonal to it.
BACKWARD_CHECK: Applying the corrected behavior to 2026-09-11: S0 would be scored +0.5 (risk-on tailwind for a beta sector on a green-futures CPI day) instead of 0; S1 would be scored −0.5 (offered barrel sets relative sign only, since the dip was ~2–3% not a >5% break) instead of −1; S3 stays −0.5 (crowded-long, correctly identified). Net leading sum = +0.5 − 0.5 − 0.5 = −0.5, × 0.9 = −0.45 → flat/mild, which would have been a direction HIT (actual flat/mild, +0.32%) and magnitude HIT. The corrected behavior converts a direction MISS into a direction HIT without disturbing the magnitude call. Backward check passes.
CONFLICT_CHECK: No conflict with active lessons. The 09-10 Energy lesson (record-close + extended run + sub-1.5% increment = asymmetric DOWN) is not contradicted — that lesson governs the RELATIVE fade, which did fire (−0.53% rel). The 09-08/09-09 magnitude-discipline lesson is honored (mild cap retained). The 08-11 live-oil verify lesson is honored (Channel 1 and live sign agreed at snapshot; the error was the persistence assumption, not the verification). The 08-12 stale-run cap and 08-14 green-oil escalation correctly did not fire. No active lesson instructs the model to score green futures as a headwind for a beta sector on a CPI day.
FALSIFIER: This lesson is falsified if, on a future session with (a) a pending high-impact macro binary, (b) all four US index futures green ≥+0.5%, and (c) a commodity/beta sector with a mildly-offered underlying (~2–3% dip, not a break), the sector ETF closes DOWN in absolute terms by more than the magnitude band. In that case the offered underlying DID force an absolute down-close against a bid tape, and S0 should not be scored as a tailwind. A secondary falsifier: if the sector's relative underperformance on such a session exceeds ~1.5% (vs the −0.53% realized here), the "caps participation only" framing understates the transmission and the S1 weight should be restored.
DIVERGENCE_VERDICT: leading_right
ACTIVE_LESSON_REVIEW: The 09-10 Energy lesson (record-close + extended run = asymmetric DOWN) FIRED correctly and its risk-management prescriptions (cap magnitude, cut conviction to 0.52, flag divergence, do not double-count S2/S4) were all correct and should be retained. The 09-08/09-09 magnitude-discipline lesson FIRED correctly (mild cap honored, magnitude HIT). The 08-11 live-oil verify lesson FIRED correctly at snapshot. The 08-14 green-oil escalation correctly did NOT fire. The 08-12 stale-run cap correctly did NOT fire. The failure is a NEW, orthogonal error: sign mis-assignment on S0 for a beta sector on a green-futures CPI day, plus an unwarranted persistence assumption on the offered barrel. No active lesson needs revision; this is a new lesson to add.
SECTOR: Energy
LESSON_END
