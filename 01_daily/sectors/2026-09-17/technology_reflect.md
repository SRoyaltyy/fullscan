# Sector Reflect — Technology — 2026-09-17

LESSON_BEGIN
ERROR_CATEGORY: B
TRIGGER_PATTERN: A sector ETF's live premarket tape and index futures independently confirm direction (PM:XLK ≥ +1%, NQ ≥ +0.5% vs prior cash) on the session AFTER an already-printed high-impact macro event, but a stale multi-horizon relative-strength veto (sector_rs_tape d1/w1 both negative) fires and forces the deterministic layer to emit flat/flat against the analyst layer's own up call.
CURRENT_BEHAVIOR: The v2 pipeline applied `sector_rs_veto_applied: True` with `sector_rs_tape: {d1: -2.05, w1: -2.07}` — leftover relative-strength lag from prior sessions — and this veto overrode a live, confirming, green premarket tape (XLK PM +1.28%, greenest of the injected sector set) and NQ +2.1% vs prev close. The morning's own binding rule from the 09-16 split lesson ("NQ independently ≥ +0.5% binds direction = up, not flat") was written precisely to prevent this, and the pipeline did not enforce it. Result: flat/flat emitted against analyst up/mild, actual +2.245% (notable). Direction MISS, magnitude MISS.
CORRECTED_BEHAVIOR: A stale multi-horizon relative-strength veto (sector_rs_tape d1/w1 negative) must NOT fire when the live premarket tape independently confirms direction. Specifically: (a) if PM:XLK ≥ +1% AND NQ ≥ +0.5% vs prior cash, the sector_rs_veto is suppressed — live tape outranks leftover RS; (b) the NQ-binding rule (NQ ≥ +0.5% → direction = up) is a hard floor that no stale RS veto may override; (c) leftover 1w/1m relative lag is a *relative* note, not an absolute-direction veto, and must never be the mechanism that flips an analyst up call to flat. When the analyst layer and the live tape agree on direction, the deterministic layer must not emit the opposite.
EVIDENCE: Outcome: XLK +2.245%, SPY +1.134%, rel +1.112% — gap-and-go, not gap-and-fade. Bloomberg 09-17: "S&P 500 up about 1%. A closely watched gauge of chipmakers climbed 3%. Treasury 10-year yields declined from the highest level since 2007, snapping an eight-day rising streak." ETF.com 09-17: "XLK rose more than 1%. Nvidia itself jumped 3.2% this morning after dropping three days straight." Morning narrative conclusion was "up / mild" with DO-INSTEAD "score and live PM agree up → keep direction." Pipeline JSON shows `sector_rs_veto_applied: True`, `sector_rs_tape: {d1: -2.05, w1: -2.07}`, `predicted_direction: flat`. Knowable-at-open: YES — every input for an up call was in hand before the bell.
LESSON_MATCH_CHECK: Matches the 09-16 split lesson (NQ ≥ +0.5% binds direction = up; do not emit flat/down against it) — that lesson was written for exactly this configuration and was not enforced by the pipeline. Also matches the cluster of 2026-09-17 candidate triggers (basic_materials, consumer_cyclical, financial, industrials, real_estate, healthcare, communication_services, utilities) — all describe the same post-printed-FOMC session where the deterministic layer emitted flat/flat or up/mild against a strongly positive index backdrop. Technology is the *most extreme* instance of this cluster (largest actual move, +2.245%) and the one where the analyst layer explicitly called up/mild and was overridden. Distinct from the 09-14 lesson (PM gap is direction, not a close extrapolant) — here the gap held and extended, so 09-14's caution was correctly applied but the veto was not.
BACKWARD_CHECK: Would this lesson have changed the 09-17 Technology call? Yes — suppressing the stale sector_rs_veto and enforcing the NQ-binding rule would have produced up/mild (analyst call), converting a direction MISS to a HIT and a magnitude MISS to a HIT (mild band vs +2.245% notable is still a band miss, but direction would be correct). Would it have hurt prior runs? Check 09-14 (predicted down/severe, actual −1.81%, dir HIT): on 09-14 the live tape was negative, so the veto-suppression condition (PM ≥ +1% AND NQ ≥ +0.5%) would not have fired — no change. Check 09-11 (predicted up/flat, actual +1.32%, dir HIT): live tape was up, veto-suppression would have reinforced the correct call — no harm. Check 09-10 (predicted flat/flat, actual −1.41%, dir MISS): live tape was negative, condition would not fire — no change. Check 09-03/09-04 (flat/flat, dir MISS on small positive prints): if live tape was green with NQ ≥ +0.5%, suppression would have produced up/mild — likely a direction HIT. No prior run is made worse by this rule.
CONFLICT_CHECK: Conflicts with the general principle that trailing relative-strength lag is a legitimate input to sector calls (S4_ETF_TAPE). Resolution: trailing RS lag remains a valid *relative* signal and a valid input to magnitude/confidence, but it must not be the mechanism that flips an analyst-confirmed direction to the opposite. The conflict is resolved by precedence: live confirming tape (PM + index futures) > stale multi-horizon RS. Also conflicts with the 09-14 lesson's caution about PM gaps fading — resolution: 09-14 governs *magnitude* (do not extrapolate PM to notable), not *direction*; the NQ-binding rule governs direction. No conflict with the 09-16 split lesson — this lesson *enforces* it.
FALSIFIER: This lesson is falsified if a future session shows PM:XLK ≥ +1% AND NQ ≥ +0.5% vs prior cash, the sector_rs_veto is suppressed per this rule, and XLK closes down or flat (direction wrong). Specifically: if suppressing the veto on a live-confirming green tape produces a direction MISS, the rule is wrong. Secondary falsifier: if a session shows the veto firing correctly (i.e., live tape green but sector closes down), the veto's precedence over live tape would be vindicated.
DIVERGENCE_VERDICT: none_flagged — the morning explicitly did not flag divergence ("No fight (tape is not negative). Trust factors. Divergence not flagged."). The divergence was not between analyst and tape; it was between the deterministic layer and both the analyst layer and the tape. This is a pipeline-enforcement failure, not a divergence-flagging failure.
ACTIVE_LESSON_REVIEW: The 09-16 split lesson was active and explicitly stated the binding rule that was violated. The 09-14 lesson (PM gap is direction, not a close extrapolant) was correctly applied to magnitude but the pipeline let it bleed into direction via the veto. The 09-10/09-11 crowding-zero lessons were correctly applied (S3 = 0 was right). The 08-12 notable-up fail correctly forbade notable — but the pipeline over-corrected to flat instead of mild. Recommendation: promote this lesson to active status for sector:Technology and all sectors, and add a hard pipeline guard: if PM:sector ≥ +1% AND NQ ≥ +0.5% vs prior cash, suppress sector_rs_veto and enforce direction = up.
SECTOR: Technology
LESSON_END
