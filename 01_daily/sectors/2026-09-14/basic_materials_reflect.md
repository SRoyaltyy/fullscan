# Sector Reflect — Basic Materials — 2026-09-14

LESSON_BEGIN
ERROR_CATEGORY: C
TRIGGER_PATTERN: A sector's own factor channel (S1) is scored at its maximum negative value by a binding composition rule (all sub-channels aligned, zero offset), but the deterministic engine applies a skill multiplier <1.0 to that channel, halving the single most accurate component of the read while the tape-anchor and llm_overlay legs simultaneously inflate the total into a more extreme magnitude band than the analyst's own component arithmetic supports.
CURRENT_BEHAVIOR: On 2026-09-14 the LLM run scored S1 = −2 (09-09 metals-co-move rule firing cleanly, all four sub-channels negative, zero offset) and reached total −4.05 → down/mild. The engine run applied skill_multipliers S1 = 0.5, cutting S1 from −2.0 to −1.0, yet still produced total −15.213 → down/notable because the tape_anchor (−6.462) and llm_overlay (−6.0) legs dominated. The realized outcome was −0.9028% absolute / −0.4567% relative — between the two bands. The engine's S1 haircut destroyed the value of the most accurate component while the anchor legs supplied the magnitude that made notable "closer" only by accident.
CORRECTED_BEHAVIOR: When a binding composition rule mandates a channel at its cap (e.g. S1 = −2 under the 09-09 all-sub-channels-negative rule), do NOT apply a sub-1.0 skill multiplier to that channel — the rule already encodes the calibration. More generally, when the analyst's component arithmetic and the engine's anchor-driven total disagree on magnitude by >2× while agreeing on every component sign, treat the disagreement as a calibration flag: report both bands, state which leg is driving the divergence (here: tape_anchor + llm_overlay, not the sector factors), and do not let the anchor mechanically set the band. The realized close exceeded the gap (−0.90% vs −0.53%), so intraday drift — not the anchor's extrapolation — was the true magnitude source.
EVIDENCE: Morning packet: LLM total −4.05 (mult 0.9, S1 = −2), engine total −15.213 (mult 0.9, S1 = −1.0 via skill_multipliers S1: 0.5, tape_anchor −6.462, llm_overlay −6.0). Outcome: XLB −0.9028%, SPY −0.4462%, rel −0.4567%, open 50.68 (−0.53% gap), close 50.49. Post-session review §2: "the engine's 0.5 multiplier on S1 halved the single most accurate component... value-destroying on this session." §4 Outlier 4 names it "the most actionable single finding of the review."
LESSON_MATCH_CHECK: Closest candidate is 2026-09-14_sector_technology_lesson.md — "the analyst's narrative magnitude band and the deterministic engine's magnitude band disagree, and the analyst's own self-audit explicitly reasons to the milder band while citing a known, named offset. The engine then overrides to the more extreme band at high confidence because the premarket tape anchor mechanically extrapolates the premarket gap to the close." That is the same structural failure (engine band override via mechanical anchor extrapolation) but the *mechanism* differs: technology's divergence came from the anchor extrapolating a gap; Basic Materials' divergence came from a skill multiplier haircutting the sector's own factor channel while the anchor inflated the total. The technology lesson is a partial match on the band-disagreement frame; this lesson adds the specific sub-mechanism of a mis-calibrated per-channel skill multiplier. No existing lesson covers the S1 skill-multiplier mis-calibration for Basic Materials.
BACKWARD_CHECK: Would this lesson have improved the 2026-09-14 call? Yes on process, marginally on outcome. Removing the 0.5 S1 haircut would have raised the engine's S1 from −1.0 to −2.0, moving the engine total further negative — i.e. *more* notable, which the realized −0.90% absolute actually favored. So the corrected behavior (no haircut on a rule-capped channel) would have made the engine's band *more* correct, not less. It would not have changed the LLM run's mild band, which was procedurally sound on the gap test. Net: the lesson sharpens the engine's calibration without degrading the analyst's.
CONFLICT_CHECK: No conflict with active rules. The 09-09 composition/magnitude rule (S1 = −2 when the 8/18 floor ban fires with all four sub-channels negative and zero offset) is *reinforced* — this lesson says do not haircut that mandated score. The 8/27 S4-cap is untouched (S4 = 0 was correct and validated). The 09-10 gap rule is untouched (gap −0.53% < 1.0%, correctly did not fire). The 09-11 pre-binary-tape rule is untouched (direction and relative-lag lean both correct). No rule is contradicted.
FALSIFIER: This lesson is falsified if, on a future Basic Materials session where a binding composition rule caps S1 at −2 with all sub-channels negative and zero offset, removing the sub-1.0 skill multiplier produces a *worse* magnitude band than keeping it — i.e. if the realized absolute move lands in the milder band despite the un-haircut S1. Concretely: if the un-haircut engine total moves the band to notable and the realized |XLB %| < 0.6%, the "do not haircut a rule-capped channel" prescription is wrong for this sector.
DIVERGENCE_VERDICT: none_flagged — the morning packet's divergence_flagged field was True in the LLM run and False in the engine run, but the realized outcome (direction down, relative lag −0.46%) validated the *factors* over the tape in both runs. No leading/futures divergence to adjudicate; the flagged divergence was internal (leading sum vs sub-gate S4), and the factors were right.
ACTIVE_LESSON_REVIEW: The binding 09-11 pre-binary-tape rule performed correctly — direction down and the explicit relative-underperformance lean both hit. The 09-10 gap rule correctly did not fire (open −0.53% < 1.0%). The 09-09 composition/magnitude rule fired correctly and its S1 = −2 was exactly right — but the engine's 0.5 skill multiplier on that channel is now flagged as mis-calibrated and should be reviewed. The 8/27 S4-cap was validated by the realized −0.4567% rel (still sub-0.5%). The 8/18 metals-as-floor ban fired cleanly. The 8/14 gold-offset correctly did not pay (gold/silver red). The 09-03 exhaustion-bounce rule was not triggered (no bounce). The 09-04 T-1-lag rule held (no stale lag copied into S4). No active rule was violated; one engine parameter (S1 skill multiplier) is newly flagged.
SECTOR: Basic Materials
LESSON_END
