# Sector Reflect — Consumer Defensive — 2026-08-25

Triage: The 2026-08-25 Consumer Defensive result is primarily a **scoreboard/grader extraction failure (category D)**, not a market-call reasoning failure. The PREDICT block explicitly says `down/mild`; the actual XLP close was **-1.06%**, so the direction call was correct. The row’s `predicted None/None` is the same recurring false-miss bug already captured by the candidate lesson. The genuine forecasting miss is only magnitude: `mild` called, while the actual relative give-back was **-1.38%**, i.e. notable.

```
LESSON_BEGIN
ERROR_CATEGORY: D
TRIGGER_PATTERN: On a scheduled trading day, a sector PREDICT block contains explicit predicted_direction and predicted_magnitude_band, but the scoreboard entry records predicted None/None and marks direction_hit/magnitude_hit False. The actual market move may confirm the predicted direction, but the grader creates a false double miss and corrupts rolling accuracy.
CURRENT_BEHAVIOR: The scoreboard ingestion does not parse the explicit `predicted_direction: down` and `predicted_magnitude_band: mild` fields from the Consumer Defensive PREDICT block, so the 2026-08-25 row is stored as `predicted None/None`, `direction_hit False`, `magnitude_hit False`. This converts a true direction hit and a true magnitude miss into a false double miss.
CORRECTED_BEHAVIOR: Grade from the PREDICT block’s explicit fields whenever they exist. For 2026-08-25 Consumer Defensive, the row should be `direction_hit: True` and `magnitude_hit: False` because actual direction was down and actual magnitude was notable vs predicted mild. Only record `None/None` when the predict source genuinely lacks those fields, and then flag a missing-prediction pipeline issue separately.
EVIDENCE: PREDICT block contains `predicted_direction: down`, `predicted_magnitude_band: mild`, `total_score: -3.15`. Actuals: XLP `-1.06%` down; SPY `+0.32%`; relative `-1.38%`; outcome labels actual magnitude as notable. Scoreboard entry incorrectly shows `predicted None/None`, `direction_hit False`, `magnitude_hit False`.
LESSON_MATCH_CHECK: Exact match with the active/recent candidate `2026-08-25_sector_consumer_defensive_lesson.md`, and the same pattern as the 2026-08-25 false-miss lessons for Communication Services, Energy, Financials, Technology, Utilities, and other sectors. This is a recurring grader/extraction defect, not a novel market lesson.
BACKWARD_CHECK: Correcting the 2026-08-25 row from false double-miss to `direction_hit True / magnitude_hit False` would change the sector’s rolling direction accuracy from about 4/9 to 5/9 if this row is included, and would remove a phantom direction miss. It would not change the true magnitude miss, which should remain a miss.
CONFLICT_CHECK: No conflict. It reinforces the existing candidate lesson for this exact sector and the broader family of 2026-08-25 scoreboard-extraction lessons. It does not conflict with the market-side lessons applied this morning: no double-counting of the risk-on shock, no re-crediting Monday’s +1.99% relative spike, WMT/DKS not allowed to drive the ETF call, and “trust factors over tape.”
FALSIFIER: This lesson would not apply if a PREDICT source genuinely lacks `predicted_direction` / `predicted_magnitude_band`, or if the scoreboard schema intentionally stores unverified predictions separately from the graded row. A checksum/fix test: compare the source PREDICT fields to the scoreboard fields on every grade; if explicit fields exist and the scoreboard still shows `None/None`, the bug is still present.
DIVERGENCE_VERDICT: leading_right — factors were trusted over the S4 tape-positive leftover, and the actual relative move (-1.38%) confirmed the fade. The divergence was explicitly flagged in the narrative even though the pipeline header showed `divergence_flagged: False`.
ACTIVE_LESSON_REVIEW: Applied market-side lessons were followed correctly: stale WMT was not re-litigated; 08-17 retail-earnings week event risk was closed; the Monday relative spike was not double-counted; the single risk-on/oil-fade/gold regime was counted once; WMT/DKS were not the driver. The only active unresolved issue is the grader/scoreboard None/None extraction bug, which this lesson re-flag.
SECTOR: Consumer Defensive
LESSON_END
```
