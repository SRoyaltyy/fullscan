# Sector Reflect — Industrials — 2026-09-23

LESSON_BEGIN
ERROR_CATEGORY: C
TRIGGER_PATTERN: A sector card nets S0–S4 to all-zero on a mixed, empty-spine session while the sector's own multi-horizon relative tape is deeply negative (1m rel ≤ −5%), rotation-out is CARRIED, and index leadership is concentrated in a non-sector complex — the card correctly refuses to mint a *down* call (per the prior-session lesson) but has no mechanism to express the symmetric *relative-outperformance* lean, so the additive framework suppresses a conjunction signal that three individually-zeroed inputs jointly imply.
CURRENT_BEHAVIOR: Each negative input (1m rel −6.87%, rotation-out CARRIED, chips leadership, oil-down, low-beta consulting as sole clean up-tape) is individually suppressed by a conditional rule (09-10 decaying-lag, 09-22 condition-not-print, 08-27 forbids-up, 08-13 blocks oil-down-as-green-light, 09-22 breadth-is-a-condition). The card then reports "no fight" and emits flat/flat with no relative lean, treating the absence of a same-morning catalyst as the absence of a thesis.
CORRECTED_BEHAVIOR: When (i) 1m rel ≤ −5%, (ii) rotation-out is CARRIED, (iii) index leadership is concentrated in a non-sector complex, and (iv) the sector's own PM quote is flat-to-slightly-negative (not a gap), the card must record an explicit **relative-outperformance lean** (sector expected to outperform the index) while keeping the *absolute* direction at flat. This is a distinct bet from an absolute up call and does not violate the 09-22 lesson (which forbids minting *down* from this tape). The card should state the lean in the divergence/self-audit block and flag whether the engine's `sector_rs_veto` (which blocks *up* calls on laggard tape) is also blocking *relative* expression.
EVIDENCE: 2026-09-23 Industrials: predicted flat/flat, actual XLI +0.0706% vs SPY −0.7202%, rel +0.7908%. Direction HIT, magnitude HIT, relative MISS (unexpressed). The card's own prose named every ingredient — "rotation-out CARRIED," "1m rel −6.87%," "chips leadership," "only clean up-tape is Consulting Services" — and the engine's `sector_rs_veto_applied: True` with tape d1 −1.65 / w1 −2.74 vetoed the up side. The outcome review's own verdict: "right for the wrong structural reason… conservatism bias in the additive framework — components zeroed individually, conjunction ignored."
LESSON_MATCH_CHECK: Partial match to `2026-09-23_sector_consumer_cyclical_lesson.md` (card's own knowable-at-open inputs lean one direction yet S0–S4 net to all-zero because each negative is individually suppressed by a conditional rule). That lesson covers the *absolute* direction suppression; this one is the *relative* variant — the inputs lean toward relative outperformance, not absolute direction, and the framework has no relative channel at all. Also adjacent to `2026-09-23_sector_healthcare_lesson.md` (all-zero card, engine emits signed direction off index_carry) but distinct: here the engine emitted flat, not a signed direction, and the miss was in the unexpressed relative dimension. No exact prior lesson covers the relative-lean gap.
BACKWARD_CHECK: Applied to 2026-09-22 (predicted down/mild, actual +0.171%, dir MISS): the 09-22 card *did* mint a down call from MAP HEAT + 1m lag + PM quote, and was wrong. The corrected behavior here would have kept absolute flat (correct) and added a relative lean — which on 09-22 (XLI +0.171% vs SPY −0.016%, rel +0.186%) would also have been correct. Applied to 2026-09-21 (predicted flat/flat, actual +0.112% vs SPY +1.55%, rel −1.44%): the corrected behavior would have added a relative *underperformance* lean — also correct. Applied to 2026-09-17/09-18 (flat/flat dir MISSes on tiny leftover-gap prints): the 1m rel condition was not ≤ −5% in those windows, so the lean would not have fired — no harm. The rule is backward-consistent and does not degrade the recent flat-call wins.
CONFLICT_CHECK: No conflict with the 09-22 lesson (forbids minting *down* from mixed T+1 + MAP HEAT + 1m lag + PM quote) — the corrected behavior keeps absolute direction at flat and only adds a relative lean. No conflict with 08-27 (1w/1m laggard forbids *up*) — a relative lean is not an absolute up call. No conflict with 09-10 (prior 1d rel on a deep laggard is decaying) — that rule governs the *1d* signal, not the 1m conjunction. Potential tension with the engine's `sector_rs_veto` (blocks up calls on laggard tape): the corrected behavior explicitly flags this for review, since a relative bet is a different instrument from an absolute direction bet. No double-count introduced: the 1m lag is still scored once, as a condition, and the lean is a *conjunction* statement, not a re-score of any single component.
FALSIFIER: If a future session presents the same conjunction (1m rel ≤ −5%, rotation-out CARRIED, non-sector leadership concentration, flat-to-slightly-negative PM quote) and XLI *underperforms* SPY (rel < 0), the relative-lean rule is falsified. Secondary falsifier: if the lean fires and the absolute direction call degrades from flat to a signed call, the rule has leaked into the absolute channel and must be tightened.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: The 09-22 Industrials lesson (do not mint down/mild from MAP HEAT + 1m lag + PM quote) was correctly applied and saved the direction/magnitude call — it should remain active and unchanged. The 09-23 consumer-cyclical lesson (all-zero card suppressing a directional lean) is the closest sibling and should be cross-referenced. The 09-23 healthcare lesson (engine emits signed direction off index_carry when card is all-zero) is adjacent but does not apply here — the engine emitted flat, not a signed direction. No active lesson is contradicted by this diagnostic; the new lesson fills a gap (relative-lean channel) rather than overriding any existing rule.
SECTOR: Industrials
LESSON_END
