# Sector Reflect — Consumer Cyclical — 2026-09-23

LESSON_BEGIN
ERROR_CATEGORY: A
TRIGGER_PATTERN: A sector card's own knowable-at-open inputs lean one direction (≥5 of ~7: red Europe, negative NQ futures, unanimous nested sub-industry heat down, weak structural 50-dma breadth, multi-horizon relative lag, Nasdaq-led futures configuration) yet the card nets S0–S4 to all-zero because each negative is individually suppressed by a conditional rule ("nested stays nested," "do not restack 1w/1m lag when S0=0," "stale clusters cannot net a live sign," "sub-gate 1d rel") whose precondition is not re-verified after the other components are zeroed.
CURRENT_BEHAVIOR: Each suppression rule is applied in isolation and treated as a one-way ratchet toward zero. The card never asks "given all these suppressions, is the residual genuinely unsigned, or have I suppressed every directional signal by construction?" The all-zero card is emitted as a safe default rather than as an earned claim, and the conditional rules (08-28 restack ban, nested-stays-nested, stale-cluster netting) are applied without checking whether their precondition (S0=0) still holds once the other legs are scored.
CORRECTED_BEHAVIOR: When the leading sum is 0, require an explicit "why is this genuinely unsigned?" justification that names the *live signed* inputs and shows they cancel. If ≥5 of the knowable-at-open signals lean one direction, the card must either (a) score the lean, or (b) name a specific live counter-signal of comparable magnitude that cancels it. Conditional suppression rules must be gated on their precondition being re-verified *after* all other components are scored — if S0 should be −1, the 08-28 "do not restack lag when S0=0" rule is void and the 1w/1m lag becomes confirmatory trend evidence, not leftover to be suppressed. Additionally: "no scheduled economic binary" ≠ "no catalyst" — a standing pre-open geopolitical headline check (UNGA, Iran, Trump–Xi, etc.) is mandatory, since a geopolitical binary is a same-session S0-class shock that is not on the economic calendar.
EVIDENCE: 2026-09-23 XLY predicted flat/flat (all S0–S4 = 0, total 1.029, conf 0.42) vs actual −1.408% / SPY −0.720% / rel −0.688% (down/notable). The card's own inputs: Europe −0.25% (red), NQ=F −0.10%, nested MAP HEAT Apparel/Dept stores/Footwear/Home Improvement all down, structural 50-dma breadth ~17%, 1w rel −2.24% / 1m rel −5.28%, Nasdaq-led futures. The Iran/UNGA "annihilate" headline was published 12:31 GMT (08:31 ET), before the open, and was absent from the research appendix (no geopolitics query run). The card's only live signed input was oil "relief," which was second-order and mis-signed (oil steadied, not offered, per the 08:05 GMT headline). Six of seven knowable-at-open signals leaned down and were netted to zero. This mirrors the 09-22 pathology in reverse: on 09-22 the engine minted a signed call the LLM correctly refused; on 09-23 the LLM minted a flat call the tape refused — the all-zero card is not a safe default.
LESSON_MATCH_CHECK: Matches candidate 2026-09-22_sector_consumer_cyclical_lesson.md (all-zero leading S0–S4 on a T+1 digestion session where ES/NQ are inside ±0.5% and v2 mints a signed official from leftover tape_anchor + index_carry) — same sector, same all-zero-card failure mode, but the 09-23 instance is the *LLM-side* version: the card itself netted to zero rather than the engine overriding it. Also matches 2026-09-23_lesson.md (scheduled hard-data release after the cash open with flat futures) as a partial — but 09-23's decisive miss was geopolitical, not the PMI. Extends both: the all-zero default is the shared root.
BACKWARD_CHECK: Would this lesson have changed the 09-23 call? Yes — requiring an explicit unsigned justification with ≥5 of 7 signals leaning down would have forced S0=−1 (Europe red + NQ negative + geopolitical headline), which voids the 08-28 precondition and lets S4=−1 (1w/1m lag confirmatory), yielding down/mild-to-notable instead of flat/flat. Would it have changed 09-22? No — on 09-22 the LLM's all-zero was audited correct and the engine minted the signed call; this lesson targets the LLM-side netting, so it is scoped correctly and does not retroactively break the 09-22 correct refusal. Would it have changed 09-16/09-17/09-18/09-21 (all flat/flat MISSes)? Possibly — those are the same all-zero-default pattern; the lesson would force a re-examination of whether the lean was genuinely unsigned, which is the correct pressure.
CONFLICT_CHECK: No conflict with active lessons. 08-28 (do not restack 1w/1m lag when S0=0) is preserved — the lesson explicitly gates it on re-verifying S0 after all components are scored, which is the rule's own stated precondition. 09-11 (stale clusters cannot net a live relief sign to negative) is preserved — the lesson does not ask stale clusters to carry the sign; it asks the *live* signals (Europe red, NQ negative, geopolitical headline, Nasdaq-led futures) to be scored. 09-03 (stale clusters cannot dominate an unprinted day) is preserved for the same reason. 09-21 (sign S0+ only when cross-asset confirms risk-on) is symmetric-extended, not contradicted. No active lesson requires the all-zero default.
FALSIFIER: If a future Consumer Cyclical card with ≥5 of 7 knowable-at-open signals leaning one direction, an explicit unsigned justification, and re-verified suppression preconditions still closes within ±0.3% of SPY (i.e., the lean was genuinely noise), this lesson is falsified. Conversely, if a card with a genuine live counter-signal of comparable magnitude (e.g., a large positive oil-relief increment plus a confirmed cross-asset risk-on certificate) nets to zero and the session is flat, the "earn the zero" requirement is over-tight and should be relaxed.
DIVERGENCE_VERDICT: none_flagged
ACTIVE_LESSON_REVIEW: Reviewed 08-28 (restack ban — preserved, gated on S0 re-verification), 09-11 (stale-cluster netting — preserved, live signals carry the sign), 09-03 (stale dominance — preserved), 09-21 (cross-asset risk-on certificate — symmetric-extended to risk-off), 09-22 (engine leftover tape_anchor minting signed calls — complementary, this is the LLM-side mirror), 08-27 (NVDA/XLK-map ban on S0=+1 — preserved), 08-11 (oil-shock — preserved; note the 09-23 oil sign flipped intraday, so the "relief" read was not durable, reinforcing the demotion of oil to a tiebreaker). No active lesson is contradicted; the new lesson adds the "earn the zero" requirement and the mandatory geopolitical pre-open check.
SECTOR: Consumer Cyclical
LESSON_END
