# Sector Reflect — Basic Materials — 2026-09-21

LESSON_BEGIN
ERROR_CATEGORY: C
TRIGGER_PATTERN: A sector ETF whose own relative tape is persistently and materially negative (1d rel ≤ −1.5%, 1w/1m rel also negative) and whose live premarket print is red while index futures are green (PM:XLB < 0 vs ES/NQ ≥ +1%), with nested MAP HEAT majority-down vs parent — the pipeline's `sector_rs_veto` + `calendar_size_gate` then flatten a correctly-signed down/mild LLM overlay into official flat/flat, producing a direction MISS on a session that closes down.
CURRENT_BEHAVIOR: The engine treats a red sector in a green index tape as a "no-edge / mixed" configuration. `sector_rs_veto_applied: True` with `sector_rs_tape: {d1: −2.08, w1: −4.78}` plus `calendar_size_gate_applied: True` compresses leading_sum −5 (S0 0, S1 −1, S2 −1, S3 0, S4 0) × mult 0.85 into total 1.904 → flat/flat, even though the LLM overlay was −2.125 (down). The LLM self-audit then compounds this by writing "Factors and live PM agree down/soft — no flag" while the pipeline's own JSON says `divergence_flagged: True`.
CORRECTED_BEHAVIOR: The RS veto is designed to prevent chasing a *green* sector into a *red* index tape. It must not be applied in the mirror configuration — a *red* sector in a *green* index tape. When (a) `sector_rs_tape.d1 ≤ −1.5%` AND (b) live PM is red while ES/NQ are green AND (c) nested HEAT is majority-down, the RS veto should **confirm** down/mild rather than flatten to flat. The DO-INSTEAD "cut conviction when score sign fights tape" rule does not apply when sign **agrees** with tape (both down). Additionally, when the LLM overlay is ≤ −2.0 and the factor logic is internally consistent (S1/S2 negative, S0 correctly zeroed), the size gate should not be permitted to compress the band below mild-down.
EVIDENCE: 2026-09-21 XLB closed −0.50% abs / −2.05% rel vs SPY +1.55%. Morning card: predicted flat/flat, leading_sum −5, overlay −2.125, `divergence_flagged: True`, `sector_rs_veto_applied: True`, `calendar_size_gate_applied: True`. Card's own call expression read "absolute down / mild, relative lag vs SPY" — correct — but the pipeline emitted flat/flat. Fifth consecutive flat/flat in the named miss cluster (09-08, 09-16, 09-17, 09-18, 09-21). Rolling last-10 dir=0.3 mag=0.2.
LESSON_MATCH_CHECK: Directly matches candidate trigger `2026-09-18_sector_basic_materials_lesson.md` — "parent PM only modestly green (<1%), nested MAP HEAT already majority-down, 1d/1w/1m relative lag is live — the model still books green ES/NQ + that PM as S0>0, nets S1–S4 to zero, and emits flat/flat." Today's variant is stronger: PM is *red* (not merely modestly green), so the 09-18 trigger's condition is exceeded, yet the same flat/flat output recurred. Also matches the cross-sector family of 09-18 lessons (Communication Services, Consumer Defensive, Healthcare, Technology, Real Estate, Utilities) where v2 tape_anchor + index_carry flatten a correctly-signed overlay. This is the BM-scope instance of that family.
BACKWARD_CHECK: Would this rule have changed prior BM calls? 09-18 (flat/flat vs −1.42%): PM was modestly green, not red — rule (b) not satisfied, so no change; that miss needs the 09-18 lesson's own fix. 09-16 (flat/flat vs −0.73%): PM condition needs re-check but the RS-veto-flattening mechanism was present — likely a second catch. 09-08 (flat/flat vs −0.95%): same mechanism family. 09-17 (flat/flat vs +0.69%): rule would have *worsened* that call (actual was up) — but 09-17's PM was green, so condition (b) fails and the rule does not fire. Net: the rule is selective and would have converted 09-21 (and likely 09-16) from MISS to HIT without touching 09-17.
CONFLICT_CHECK: Conflicts with the DO-INSTEAD note "when score sign fights tape, cut conviction; prefer flat/mild." Resolution: that rule is conditioned on sign *fighting* tape. Here sign *agreed* with tape (factors down, PM red, HEAT-down, rel negative) — the rule was misapplied. No conflict with 09-18 HEAT-down binding rule (that rule says "prefer down/mild over another flat" — this lesson enforces it). No conflict with 8/27 S4-cap (still no confirmed-up). No conflict with 09-04 T−1-lag (S4 stays 0; the fix is in the veto/gate layer, not S4).
FALSIFIER: If on a future BM session with `sector_rs_tape.d1 ≤ −1.5%`, PM:XLB red, ES/NQ green, and nested HEAT majority-down, XLB closes **green or flat (≥ −0.2%)**, this lesson is falsified — the RS veto's flattening would have been correct and the red-PM/green-index configuration is genuinely no-edge. Secondary falsifier: if the pipeline emits down/mild under this configuration and XLB closes up ≥ +0.5%, the corrected behavior over-corrects.
DIVERGENCE_VERDICT: leading_right — the LLM overlay (−2.125, down) and the leading S0–S3 read were correct; the pipeline's RS veto + size gate flattened them. The divergence flag was raised (`divergence_flagged: True`) but resolved in the wrong direction.
ACTIVE_LESSON_REVIEW: 09-18 HEAT-down binding rule — correctly applied (S0=0, S1=−1, S2=−1) but its "prefer down/mild over another flat" instruction was overridden by the veto/gate layer; this lesson enforces it. 09-17 residual-mild-up — correctly OFF (PM red). 09-16 transmission haircut — correctly applied as process. 09-15 nested-bid — correctly OFF (Cu HEAT down). 8/25 up-ban — correctly ON. 8/27 S4-cap — correctly ON. 09-04 T−1-lag — correctly ON. 8/14 gold sleeve — correctly sleeve-only. China/gold split — correctly applied. DO-INSTEAD last-three-BM-losses — **misapplied**: written for sign-fights-tape, invoked on a sign-agrees-with-tape session. This misapplication is the proximate cause of the fifth flat/flat.
SECTOR: Basic Materials
LESSON_END
