# Sector Reflect — Consumer Defensive — 2026-09-21

LESSON_BEGIN
ERROR_CATEGORY: C
TRIGGER_PATTERN: A low-beta defensive sector ETF (XLP/XLU/XLV-like) posts a net-negative leading S0–S4 card (funding-source / rotation-out object scored negative, no FTS spine, live PM red and already inside the mild-down band) on a strong risk-on session (index futures ≥ +1%, growth/tech leading by a wide margin, oil offered, VIX contango). The v2 engine's `index_carry` leg (large positive, e.g. +3.2) plus `sector_rs_veto`/`tape_anchor` then arithmetically flatten the negative leading sum (e.g. −3.5) into an official flat/flat call, while the LLM overlay and prose both say down. The engine's index-carry machinery systematically fights correct negative defensive reads in risk-on regimes.
CURRENT_BEHAVIOR: The pipeline emits `predicted_direction: flat, predicted_magnitude_band: flat` from `total_score: -1.24`, where `index_carry: 3.218` (general 12.871) pulls the leading sum of −3.5 up to −1.24, and `sector_rs_veto_applied: True` with `sector_rs_tape: {d1: 1.44, w1: 0.52}` further dampens the negative. The analyst prose explicitly warned "If v2 tape_anchor tries up off ES +1.35%, that is the 09-17/09-18 error class — reject; trust factors," yet the official output still printed flat. This is the third consecutive session (09-17, 09-18, 09-21) where the engine flattened a correct negative defensive read in a risk-on regime.
CORRECTED_BEHAVIOR: When the LLM overlay sign and the live PM sign AGREE and both are negative for a defensive sector, and the leading S0–S4 sum is net-negative, the `index_carry` leg must NOT be allowed to pull the official direction to flat/up. Promote the morning's own "reject the engine, trust factors" instruction from prose to a scored override: if `sign(overlay) == sign(PM) == sign(leading_sum)` and `sign(index_carry) != sign(leading_sum)`, cap the index_carry contribution so the official direction preserves the leading-sum sign. Additionally, when the board shows an escalated anti-FTS input (e.g. NQ lead 77 bp vs prior sessions' 36–39 bp), escalate S0 magnitude rather than holding it at the same −1 as weaker prior sessions.
EVIDENCE: 2026-09-21 XLP actual −1.087% / SPY +1.552% / rel −2.639% (notable down). Morning card: S0 −1, S1 −0.5, S2 0, S3 0, S4 0, leading_sum −3.5, index_carry +3.218, total_score −1.24 → official flat/flat. Analyst prose: "Official lean is down/mild, not up, not flat." Scoreboard: dir MISS, mag MISS. Prior sessions: 09-18 predicted up/mild vs actual −0.826% (dir MISS); 09-17 predicted up/mild vs +0.192% (dir HIT on 19 bp, mag MISS). Candidate lesson `2026-09-18_sector_consumer_defensive_lesson.md` already describes this exact pattern; candidate lesson `2026-09-21_sector_basic_materials_lesson.md` describes the same `sector_rs_veto` + `calendar_size_gate` flattening mechanism in a different sector.
LESSON_MATCH_CHECK: MATCHES `2026-09-18_sector_consumer_defensive_lesson.md` (same sector, same engine failure mode: v2 tape_anchor + index_carry emitting up/flat against a net-negative leading card on a post-event risk-on rebound). Also MATCHES `2026-09-21_sector_basic_materials_lesson.md` (same `sector_rs_veto` + `calendar_size_gate` flattening mechanism, different sector). This is a recurrence, not a novel error — the 09-18 lesson was not applied as a scored override on 09-21.
BACKWARD_CHECK: Had the corrected behavior been applied on 09-18, the official call would have been down (not up/mild), converting a dir MISS to a dir HIT. Had it been applied on 09-21, the official call would have been down (not flat), converting a dir MISS to a dir HIT. On 09-17, the corrected behavior would have preserved the up/mild call (since PM was small-green leftover beta, not red), so no harm. The correction is strictly beneficial across the three-session window.
CONFLICT_CHECK: No conflict with active lessons. The 09-11 lesson ("no FTS bid is a relative negative, not S0=0") is reinforced, not contradicted. The 08-28 lesson (do not copy paid tape into S2/S4) is orthogonal — this correction concerns the engine's index_carry leg, not the analyst's S2/S4 scoring. The 09-17/09-18 "reject engine, trust factors" instruction is the direct antecedent and is being promoted from prose to scored override.
FALSIFIER: If on a future session with a net-negative leading S0–S4 card for a defensive sector, red PM, and a strong risk-on tape (index futures ≥ +1%, growth leading), the sector ETF closes UP or flat (|pct| < 0.3%), then the corrected behavior (preserving the negative leading-sum sign) would have produced a dir MISS, falsifying the claim that index_carry systematically fights correct negative defensive reads. Conversely, if the engine's index_carry leg is shown to have positive predictive value for defensive sectors in risk-on regimes (i.e., defensive ETFs systematically mean-revert up on such days), the correction should be withdrawn.
DIVERGENCE_VERDICT: leading_right
ACTIVE_LESSON_REVIEW: The 09-18 consumer defensive lesson was the correct diagnosis but was not operationalized as a scored override — it remained prose. The 09-21 basic materials lesson independently identified the same `sector_rs_veto` + `calendar_size_gate` flattening mechanism. Both should be consolidated into a single cross-sector rule: when overlay sign, PM sign, and leading-sum sign agree against index_carry, the official direction must preserve the leading-sum sign. The magnitude lesson (do not let single-session input-cost relief or nested sleeve strength cap a defensive sector at "mild" on a ≥+1.5% SPY day) should be added as a separate magnitude rule.
SECTOR: Consumer Defensive
LESSON_END
