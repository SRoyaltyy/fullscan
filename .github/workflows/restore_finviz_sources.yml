name: Restore Finviz-wired sources (one-shot)
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Write sources from embedded b64
        run: |
          python3 << 'PY'
          import base64
          from pathlib import Path
          # content will be injected by next step or we use a different approach
          print('placeholder - use apply from local')
          PY
