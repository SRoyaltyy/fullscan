name: Collect SEC Filings & Insider Trades

on:
  # schedule disabled 2026-08-29 — not in live pipeline
  # schedule:
  #   - cron: '0 14 * * 1-5'  # Weekdays at 2:00 PM UTC
  workflow_dispatch:
  workflow_call:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run SEC filings collector
        env:
          EDGAR_IDENTITY: ${{ secrets.EDGAR_IDENTITY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FULLSCAN_DB_OPTIONAL: "1"
        run: python collectors/sec_filings_collector.py
