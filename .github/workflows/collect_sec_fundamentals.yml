name: Collect SEC Fundamentals

on:
  # schedule disabled 2026-08-29 — not in live pipeline
  # schedule:
  #   - cron: '0 11 * * 0'   # Every Sunday at 11:00 AM UTC
  workflow_dispatch:
  workflow_call:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run SEC fundamentals collector
        env:
          EDGAR_IDENTITY: ${{ secrets.EDGAR_IDENTITY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FULLSCAN_DB_OPTIONAL: "1"
        run: python collectors/sec_fundamentals_collector.py
