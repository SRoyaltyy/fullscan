name: Restore catalyst_analysis.py

# Disabled auto-push. The collector is edited in-repo now.
# Keep dispatch only so we do not startup-fail on every related push.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "collector is Grok-native; no restore needed"
